# 응급의료 지역 위험도 사용자 해석 기준 보고서 — 과거 분포 해석 스냅샷

> [!IMPORTANT]
> 이 보고서의 구성점수 분위수와 사례 계산은 갱신 전 124개 완료지역 데이터로 작성되어 보관 중이다. 현재 검증 산출물은 NEMC 534개 기관·219개 지역을 기준으로 하며, 행정안전부 2026년 7월 인구를 전 지역에 연결한 결과 `regionRisk` 완료 196개·결측 23개 지역이다. 최신 수치는 `data/region_risk_final.csv`와 `README.md`를 우선한다.
>
> 종합위험도 등급은 분위수가 아니라 프론트와 백엔드가 공유하는 고정 기준이다. 현재 기준은 `20 이하=매우낮음`, `20 초과 35 이하=낮음`, `35 초과 50 이하=보통`, `50 초과 65 이하=높음`, `65 초과=매우높음`이며, 아래 10.2절에 현재 분포를 반영했다. 그 밖의 분위수 표는 과거 124개 지역 스냅샷이므로 재분석 전 현재 분포값으로 인용하지 않는다.
>
> 현행 PART 4 회귀는 전문의 0명 지역이 탈락하지 않도록 의료진 변수에 `의료진부족점수`를 사용한다. 완료지역 196개 전체가 포함되며 R²는 `0.8613975747`, MAE는 `3.0653038392`이다.
>
> HIRA 중복 식별자 2건은 공식 HIRA 기관 페이지로 수동검증해 고양 명지병원(`A2100042`) 12명, 제천 명지병원(`A2300009`) 7명으로 분리했다. 새강병원(`A2502742`) 좌표도 보완되어 현재 NEMC 병원 좌표 결측은 0개다.
>
> 지도 경계는 통계청 SGIS 기반 `admdongkor` `20260701` `light` 단순화 자료다. 데이터 라이선스는 CC BY 4.0과 공공누리 제1유형이며, 법적·측량용 공식 원본이 아니다.

## 1. 분석 목적

본 보고서는 `region_risk_final.csv`에서 산출된 지역별 응급의료 위험도와 네 개의 구성점수를 사용자가 이해할 수 있는 형태로 해석하기 위한 기준을 정의한다.

이 보고서를 작성할 당시 데이터는 총 **219개 지역**을 포함했으며, 이 중 병상포화도점수, 접근성점수, 인구대비병상점수, 의료진부족점수 네 항목이 모두 존재하여 `regionRisk`가 최종 산출된 지역은 **124개**였다.

당시 나머지 **95개 지역**은 적어도 하나의 구성점수가 결측이기 때문에 최종 위험도를 산출하지 않았다. 이 경우 결측값을 0점이나 임의의 값으로 대체하지 않고 `원천데이터부족`으로 처리하였다.

본 분석의 목적은 단순히 0~100점의 수치를 제시하는 것이 아니다.

각 점수가 실제 분석지역의 분포에서 어느 수준에 위치하는지 확인하고, 이를 바탕으로 사용자가 다음과 같이 이해할 수 있는 기준을 만드는 것을 목적으로 한다.

- 해당 점수가 높은 수준인지 낮은 수준인지
- 해당 점수가 실제로 무엇을 의미하는지
- 해당 지역에서 어떤 요소가 주요 위험요인인지
- 최종 `regionRisk`가 왜 해당 점수로 산출되었는지

이를 최종 대시보드의 지역별 위험도 설명에 활용하고자 한다.

> **주의:** 본 보고서에서 제시하는 구성점수 단계는 의료적·법적 임계값이 아니다. 보고서 작성 당시 `region_risk_final.csv`에서 최종 위험도가 산출된 124개 지역의 실제 데이터 분포를 기준으로 만든 **상대적 해석 기준**이다.

---

# 2. 분석 대상 변수

`region_risk_final.csv`에서 사용자에게 제공할 핵심 변수는 다음과 같다.

| 변수 | 의미 | 점수가 높을수록 |
|---|---|---|
| 병상포화도점수 | 지역 내 응급실 병상의 포화 부담 | 병상 포화 부담이 큼 |
| 접근성점수 | 응급의료기관 접근의 취약 정도 | 응급의료기관 접근이 어려움 |
| 인구대비병상점수 | 지역 인구에 비해 응급병상이 부족한 정도 | 인구 대비 병상 공급 부담이 큼 |
| 의료진부족점수 | 응급의학과 전문의 부족 정도 | 의료진 부족이 심함 |
| `regionRisk` | 네 구성점수를 통합한 종합 위험도 | 종합적인 응급의료 취약도가 큼 |

중요한 점은 네 구성점수 모두 **위험 방향 점수**라는 것이다.

즉 점수가 높을수록 좋은 것이 아니라, 해당 요소의 취약성이 크다는 의미이다.

---

# 3. 최종 위험도 산출 방식

최종 지역 위험도인 `regionRisk`는 다음과 같이 계산된다.

```text
regionRisk
= 0.35 × 병상포화도점수
+ 0.30 × 접근성점수
+ 0.20 × 인구대비병상점수
+ 0.15 × 의료진부족점수
```

각 구성요소의 가중치는 다음과 같다.

| 구성요소 | 가중치 |
|---|---:|
| 병상포화도점수 | 35% |
| 접근성점수 | 30% |
| 인구대비병상점수 | 20% |
| 의료진부족점수 | 15% |

따라서 `regionRisk`는 네 점수의 단순 평균이 아니다.

예를 들어 병상포화도점수가 매우 높더라도 접근성점수와 의료진부족점수가 매우 낮다면 최종 위험도는 병상포화도점수보다 낮게 산출될 수 있다.

반대로 병상포화도가 상대적으로 낮더라도 접근성과 의료진 부족이 동시에 매우 취약하다면 종합 위험도는 높아질 수 있다.

---

# 4. 사용자 해석 기준을 만드는 방법

## 4.1 분석 대상

해석 기준을 만들 때는 전체 219개 지역이 아니라 `산출상태 = 완료`인 **124개 지역만 사용하였다.**

최종 위험도가 없는 95개 지역은 적어도 하나의 구성점수가 결측이기 때문에 동일한 조건에서 지역 간 종합위험도를 비교하기 어렵다.

따라서 본 보고서의 사용자 해석 기준은 **네 구성점수가 모두 확보된 당시 124개 지역의 데이터 분포**를 기준으로 한다.

---

## 4.2 단순 20점 간격을 사용하지 않은 이유

모든 구성점수는 0~100점이지만 다음과 같이 단순하게 구분하는 방식은 사용하지 않았다.

```text
0~20
20~40
40~60
60~80
80~100
```

그 이유는 지표별 실제 데이터 분포가 서로 크게 다르기 때문이다.

예를 들어 접근성점수는 평균이 28.59점이지만 중앙값은 7.19점이다.

즉 상당수 지역의 접근성점수가 낮은 범위에 몰려 있고 일부 접근 취약지역이 높은 값을 가지는 형태이다.

따라서 단순히 20점 단위로 구분하면 실제 전국 분석지역에서 해당 지역이 어느 정도 위치인지 제대로 설명하기 어렵다.

---

## 4.3 분위수 기반 상대평가

각 구성점수에 대해 완료된 124개 지역의 실제 분포를 계산하고 다음 기준을 사용하였다.

- P20: 하위 20% 경계
- P40: 하위 40% 경계
- P60: 상위 40% 진입 경계
- P80: 상위 20% 진입 경계

이를 이용해 각 구성점수를 다섯 단계로 해석한다.

따라서 본 보고서의 `매우 낮음`, `보통`, `매우 높음` 등의 표현은 절대적인 의료 기준이 아니라 **현재 분석지역 내 상대적 위치**를 의미한다.

---

# 5. 완료 지역의 실제 점수 분포

보고서 작성 당시 `region_risk_final.csv`에서 `산출상태 = 완료`인 124개 지역을 대상으로 계산한 결과는 다음과 같다.

| 지표 | 평균 | 중앙값 | P20 | P40 | P60 | P80 | 최대 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 병상포화도점수 | 39.68 | 40.00 | 20.00 | 34.22 | 45.76 | 55.56 | 100.00 |
| 접근성점수 | 28.59 | 7.19 | 1.90 | 5.35 | 30.89 | 59.73 | 100.00 |
| 인구대비병상점수 | 30.07 | 21.53 | 7.75 | 18.04 | 28.48 | 47.80 | 100.00 |
| 의료진부족점수 | 48.16 | 21.45 | 6.21 | 12.20 | 91.32 | 100.00 | 100.00 |
| `regionRisk` | 35.70 | 32.65 | 22.00 | 28.60 | 37.61 | 47.48 | 88.11 |

이 결과에서 특히 접근성과 의료진부족점수의 분포가 다른 지표와 크게 다르다는 것을 확인할 수 있다.

---

# 6. 병상포화도점수 해석 기준

## 6.1 지표의 의미

병상포화도점수는 **현재 응급실 병상이 얼마나 포화되어 있는지를 나타내는 점수**이다.

원천 포화율은 다음과 같이 계산한다.

```text
포화율
= (기준 응급실 병상 - 가용 응급실 병상)
  / 기준 응급실 병상 × 100
```

즉 현재 사용할 수 있는 응급실 병상이 적을수록 포화율이 높아지고, 최종 병상포화도점수 역시 높아진다.

따라서:

**높은 점수 = 병상 포화 부담이 큼**

으로 해석한다.

---

## 6.2 데이터 기반 단계

당시 완료된 124개 지역의 실제 분포를 기준으로 다음과 같이 해석한다.

| 병상포화도점수 | 단계 | 사용자 해석 |
|---|---|---|
| 0 ≤ 점수 < 20.00 | 매우 낮음 | 병상 포화 부담이 분석지역 중 매우 낮은 편 |
| 20.00 ≤ 점수 < 34.22 | 낮음 | 병상 포화 부담이 비교적 낮은 편 |
| 34.22 ≤ 점수 < 45.76 | 보통 | 분석지역의 중간 수준 |
| 45.76 ≤ 점수 < 55.56 | 높음 | 병상 포화 부담이 높은 편 |
| 55.56 ≤ 점수 | 매우 높음 | 병상 포화 부담이 분석지역 중 매우 높은 편 |

예를 들어 병상포화도점수가 90점이라면 단순히 `90/100`이라는 의미뿐 아니라, 현재 완료지역의 분포에서 **매우 높은 병상포화 부담을 가진 지역**이라고 설명할 수 있다.

다만 이것은 특정 병원이 의료적으로 위험하다는 판정이 아니라 **지역 단위의 상대적인 병상포화 취약성**을 의미한다.

---

# 7. 접근성점수 해석 기준

## 7.1 지표의 의미

접근성점수는 해당 지역에서 응급의료기관에 접근하기 얼마나 어려운지를 나타내는 점수이다.

따라서 다른 일반적인 편의성 점수와 달리:

**낮은 점수 = 접근성이 양호함**

**높은 점수 = 접근성이 취약함**

으로 해석한다.

---

## 7.2 실제 데이터 분포

접근성점수의 실제 분포는 다음과 같다.

```text
평균      28.59
중앙값     7.19
P20        1.90
P40        5.35
P60       30.89
P80       59.73
최대     100.00
```

평균은 28.59점인데 중앙값은 7.19점으로 큰 차이가 존재한다.

이는 많은 지역이 낮은 접근성 위험점수에 몰려 있고, 일부 접근성이 매우 취약한 지역의 높은 점수가 평균을 끌어올리고 있음을 의미한다.

따라서 접근성점수는 특히 20점 단위의 단순 구간보다 실제 데이터 분포를 기준으로 해석하는 것이 적절하다.

---

## 7.3 데이터 기반 단계

| 접근성점수 | 단계 | 사용자 해석 |
|---|---|---|
| 0 ≤ 점수 < 1.90 | 매우 양호 | 응급의료기관 접근 취약도가 분석지역 중 매우 낮음 |
| 1.90 ≤ 점수 < 5.35 | 양호 | 응급의료기관 접근성이 비교적 양호 |
| 5.35 ≤ 점수 < 30.89 | 보통 | 접근 취약도가 분석지역의 중간 범위 |
| 30.89 ≤ 점수 < 59.73 | 취약 | 응급의료기관 접근이 어려운 편 |
| 59.73 ≤ 점수 | 매우 취약 | 접근 취약도가 분석지역 중 매우 높은 편 |

예를 들어 접근성점수가 1점이라면 `100점 만점 중 1점`이라고만 설명하는 것이 아니라:

> 응급의료기관 접근 취약도가 분석지역 중 매우 낮아, 상대적으로 접근성이 매우 양호한 지역

이라고 해석할 수 있다.

---

# 8. 인구대비병상점수 해석 기준

## 8.1 지표의 의미

인구대비병상점수는 해당 지역의 인구 규모에 비해 응급의료 병상이 얼마나 부족한지를 나타낸다.

따라서:

**낮은 점수 = 인구 대비 병상 공급 부담이 낮음**

**높은 점수 = 인구 대비 병상 공급 부담이 큼**

으로 해석한다.

---

## 8.2 실제 데이터 분포

```text
평균      30.07
중앙값    21.53
P20        7.75
P40       18.04
P60       28.48
P80       47.80
최대     100.00
```

---

## 8.3 데이터 기반 단계

| 인구대비병상점수 | 단계 | 사용자 해석 |
|---|---|---|
| 0 ≤ 점수 < 7.75 | 매우 양호 | 인구 대비 병상 공급 부담이 매우 낮은 편 |
| 7.75 ≤ 점수 < 18.04 | 양호 | 인구 대비 병상 공급이 비교적 양호 |
| 18.04 ≤ 점수 < 28.48 | 보통 | 분석지역의 중간 수준 |
| 28.48 ≤ 점수 < 47.80 | 부족 | 인구에 비해 병상 공급 부담이 높은 편 |
| 47.80 ≤ 점수 | 매우 부족 | 인구 대비 병상 부족 부담이 분석지역 중 매우 높은 편 |

이 지표 역시 실제 병상 개수 자체만 의미하지 않는다.

지역별 인구 규모를 함께 고려한 상대적인 병상 공급 부담을 나타내는 지표이다.

---

# 9. 의료진부족점수 해석 기준

## 9.1 지표의 의미

의료진부족점수는 응급실 병상 규모와 응급의학과 전문의 현황을 이용하여 **전문의 부족 정도**를 나타낸다.

따라서:

**낮은 점수 = 의료진 부족 부담이 낮음**

**높은 점수 = 의료진 부족 부담이 큼**

으로 해석한다.

---

## 9.2 실제 데이터 분포

```text
평균      48.16
중앙값    21.45
P20        6.21
P40       12.20
P60       91.32
P80      100.00
최대     100.00
```

이 변수는 다른 변수보다 분포가 특이하다.

특히 상위 지역에서 100점이 반복적으로 나타난다.

현재 산식에서는 실제 응급의학과 전문의가 0명인 지역의 의료진부족점수를 100점으로 처리하고 있기 때문에 이러한 분포가 발생할 수 있다.

---

## 9.3 해석 시 주의점

의료진부족점수는 P80 자체가 100점이기 때문에 다른 변수처럼 분위수만으로 깔끔한 다섯 구간을 만드는 데 한계가 있다.

따라서 최종 대시보드에서는 점수와 함께 가능하면 다음 원천정보를 제공하는 것이 좋다.

- 응급의학과 전문의 수
- 병상 대비 전문의 부족비율
- 의료진부족점수
- HIRA 매칭 품질

특히 원천자료에서 실제 응급의학과 전문의가 0명인 것이 확인된 경우:

> 의료진부족점수 100점

만 표시하기보다:

> **응급의학과 전문의 0명 확인 · 의료진 부족 매우 취약**

과 같이 원인을 함께 제공하는 것이 사용자에게 더 명확하다.

---

# 10. 종합위험도 `regionRisk` 해석 기준

## 10.1 지표의 의미

`regionRisk`는 다음 네 요소를 통합한 종합적인 지역 응급의료 위험도이다.

- 병상포화도
- 접근성
- 인구 대비 병상
- 의료진 부족

따라서 특정 요소 하나만으로 결정되지 않는다.

---

## 10.2 현행 위험등급 확인

현재 `region_risk_final.csv`에는 숫자 `위험등급`과 사용자 표시용 `위험등급명`이 함께 존재한다.

보고서 갱신 당시 완료지역 126개는 다음과 같이 구분되어 있었다. 경계값은 낮은 단계에 포함된다.

| regionRisk | 위험등급 | 현재 데이터의 지역 수 | 권장 사용자 표현 |
|---|---:|---:|---|
| 0 ≤ 점수 ≤ 20 | 1 | 21 | 매우낮음 |
| 20 < 점수 ≤ 35 | 2 | 51 | 낮음 |
| 35 < 점수 ≤ 50 | 3 | 35 | 보통 |
| 50 < 점수 ≤ 65 | 4 | 11 | 높음 |
| 65 < 점수 ≤ 100 | 5 | 8 | 매우높음 |

`regionRisk`는 구성점수 분위수와 별개로 위 고정 위험등급을 사용한다. 백엔드의 `위험등급`·`위험등급명`과 프론트의 색상·라벨도 같은 구간을 사용해야 한다.

단, 1등급 `매우낮음`을 `안전`이라고 표현하는 것은 피한다.

1등급 역시 실제 의료적으로 안전하다는 의미가 아니라 현재 네 가지 지표를 종합했을 때 **상대적으로 위험도가 낮다는 의미**이기 때문이다.

---

# 11. 실제 사례: 서울특별시 강남구

강남구의 실제 분석 결과는 다음과 같다.

| 지표 | 점수 | 데이터 기반 해석 |
|---|---:|---|
| 병상포화도점수 | 90.48 | 매우 높음 |
| 접근성점수 | 1.26 | 매우 양호 |
| 인구대비병상점수 | 25.63 | 보통 |
| 의료진부족점수 | 6.19 | 매우 양호 |
| `regionRisk` | 38.10 | 위험등급 3 · 보통 |

---

## 11.1 병상포화도 해석

강남구의 병상포화도점수는 **90.48점**이다.

완료지역의 병상포화도 P80은 55.56점이므로 90.48점은 상위 20% 경계를 크게 초과한다.

따라서:

> **병상포화 부담이 매우 높은 지역**

으로 해석한다.

---

## 11.2 접근성 해석

강남구의 접근성점수는 **1.26점**이다.

접근성 P20이 1.90점이므로 강남구는 접근 취약도가 하위 20% 수준에 해당한다.

접근성점수는 낮을수록 좋은 지표이므로:

> **응급의료기관 접근성이 매우 양호한 지역**

으로 해석할 수 있다.

---

## 11.3 인구 대비 병상 해석

강남구의 인구대비병상점수는 **25.63점**이다.

P40 18.04와 P60 28.48 사이에 위치하므로:

> **인구 대비 병상 공급 부담은 분석지역의 중간 수준**

으로 해석한다.

---

## 11.4 의료진 부족 해석

강남구의 의료진부족점수는 **6.19점**이다.

P20 6.21보다 낮은 수준이므로:

> **의료진 부족 부담이 분석지역 중 매우 낮은 편**

으로 해석할 수 있다.

---

## 11.5 최종 위험도 계산

강남구의 최종 위험도는 다음과 같이 계산된다.

```text
병상포화도
90.4762 × 0.35
= 31.6667

접근성
1.2645 × 0.30
= 0.3794

인구 대비 병상
25.6303 × 0.20
= 5.1261

의료진 부족
6.1888 × 0.15
= 0.9283
```

이를 모두 더하면:

```text
31.6667
+ 0.3794
+ 5.1261
+ 0.9283
= 약 38.10
```

따라서 강남구의 최종 `regionRisk`는 **38.10점**이다.

---

## 11.6 강남구 결과가 의미하는 것

강남구의 병상포화도만 보면 90.48점으로 매우 높은 수준이다.

하지만 접근성점수는 1.26점, 의료진부족점수는 6.19점으로 매우 낮다.

따라서 강남구의 결과는:

> 병상포화 문제는 매우 크게 나타나지만, 의료기관까지의 접근성과 의료진 확보 측면에서는 상대적으로 취약성이 낮은 지역

이라고 해석할 수 있다.

즉 **병상포화도 90.48점 = 강남구 전체 응급의료 위험도가 90점이라는 의미가 아니다.**

병상포화는 강남구의 여러 위험요인 중 하나이며 다른 요소를 종합했을 때 최종 위험도는 38.10점으로 산출된다.

---

# 12. 대시보드에서의 사용자 표시 예시

최종 서비스에서는 다음과 같이 보여주는 것을 제안한다.

```text
서울특별시 강남구

종합 응급의료 위험도
38.10 / 100
위험등급 3 · 보통

병상포화도        90.48    매우 높음
접근성             1.26    매우 양호
인구 대비 병상    25.63    보통
의료진 부족        6.19    매우 양호

주요 위험요인
병상포화
```

그리고 아래에 사용자용 설명을 제공한다.

> 강남구의 종합 응급의료 위험도는 **38.1점으로 3등급·보통**입니다. 현재 병상 포화 부담은 분석지역 중 매우 높은 수준이지만, 응급의료기관 접근성과 의료진 확보 측면은 상대적으로 양호합니다. 인구 대비 병상 부담은 중간 수준입니다. 따라서 강남구는 구조적인 접근성 문제보다는 **현재 병상 포화가 주요 위험요인으로 나타나는 지역**으로 해석할 수 있습니다.

이와 같이 단순한 숫자가 아니라 **점수 + 단계 + 원인 + 설명**을 함께 제공한다.

---

# 13. 주요 위험요인 자동 판별 기준

지역마다 `regionRisk`가 높은 이유는 다를 수 있다.

따라서 사용자에게 해당 지역의 **주요 위험요인**을 함께 제공할 필요가 있다.

예를 들어:

```text
주요 위험요인: 병상포화
```

또는:

```text
주요 위험요인: 접근성 · 의료진 부족
```

과 같이 표시할 수 있다.

---

## 13.1 단순 최고점 방식의 문제

다음처럼 네 구성점수 중 가장 큰 숫자를 고르는 방식은 적절하지 않다.

```text
병상포화도        50
접근성            40
인구 대비 병상    30
의료진 부족       20

→ 50이 가장 크므로 병상포화가 가장 위험?
```

각 지표의 실제 분포가 서로 다르기 때문이다.

예를 들어 병상포화도 중앙값은 40점이지만 접근성 중앙값은 약 7점이다.

따라서 접근성 40점은 실제 분포에서는 상당히 높은 취약수준인데 단순 숫자 비교에서는 병상포화도 50점보다 낮게 보인다.

---

## 13.2 백분위 기반 주요 위험요인 판별 제안

각 지역의 네 점수를 각각 해당 변수의 전국 완료지역 분포에서 **백분위(percentile)**로 변환한다.

예를 들어:

```text
병상포화도        전국 95 percentile
접근성            전국 10 percentile
인구 대비 병상    전국 50 percentile
의료진 부족       전국 20 percentile
```

이라면 가장 높은 백분위에 해당하는 병상포화를 해당 지역의 주요 위험요인으로 선택한다.

이를 통해 서로 분포가 다른 네 지표를 동일한 상대적 기준에서 비교할 수 있다.

---

# 14. 최종 사용자 해석 구조

지역별 분석 결과는 다음 순서로 제공하는 것을 제안한다.

```text
1. regionRisk 원점수
        ↓
2. 종합 위험등급
        ↓
3. 네 구성점수
        ↓
4. 각 구성점수의 상대적 단계
        ↓
5. 주요 위험요인
        ↓
6. 사용자용 자동 설명
```

예를 들어:

```text
경상북도 A지역

종합위험도
57.2 / 100

위험등급
4 · 높음

병상포화도
42.1 · 보통

접근성
72.5 · 매우 취약

인구 대비 병상
31.0 · 부족

의료진 부족
100 · 매우 부족

주요 위험요인
접근성 · 의료진 부족
```

이후:

> 해당 지역은 병상포화 자체보다 응급의료기관까지의 접근성과 응급의학과 전문의 부족 측면에서 상대적으로 높은 취약성을 보이고 있습니다.

와 같은 설명을 자동으로 생성할 수 있다.

---

# 15. 지도 시각화 적용

대한민국 지도에서는 시군구별 `regionRisk`를 색상으로 표현할 수 있다.

사용자가 특정 지역을 선택하면 해당 지역의 상세 분석 결과를 제공한다.

예:

```text
대한민국 지도
      ↓
경상북도 선택
      ↓
시군구별 위험도 표시
      ↓
특정 시군구 클릭
      ↓
regionRisk
위험등급
병상포화도
접근성
인구 대비 병상
의료진 부족
주요 위험요인
지역 해석 문장
```

이 방식은 단순히 지도에서 위험지역을 색으로 보여주는 것을 넘어 **왜 해당 지역이 해당 색으로 표시되었는지 설명할 수 있다는 장점**이 있다.

---

# 16. 결측지역 표시 기준 — 보고서 작성 당시 스냅샷

전체 219개 지역 중 최종 위험도가 산출된 지역은 124개이다.

나머지 95개 지역은 적어도 하나의 구성점수가 결측이므로 `regionRisk`를 계산하지 않았다.

이 지역들은 지도에서 저위험 지역처럼 표시해서는 안 된다.

예를 들어:

```text
회색
데이터 부족
```

등 별도의 상태로 표현하는 것이 적절하다.

사용자가 해당 지역을 클릭하면 다음과 같이 표시할 수 있다.

> **현재 종합위험도를 산출하기 위한 원천데이터가 충분하지 않습니다.**

그리고 어떤 데이터가 부족한지 함께 보여준다.

```text
병상포화도        데이터 있음
접근성            데이터 있음
인구 대비 병상    데이터 있음
의료진 부족       데이터 부족

종합위험도
산출 불가
```

이렇게 하면 데이터가 없다는 사실을 `위험이 낮음`으로 잘못 해석하는 문제를 방지할 수 있다.

---

# 17. HIRA 결측 보완 이후 재분석

현재 의료진 관련 데이터에는 HIRA 기관 자동매칭 과정에서 발생한 결측이 존재한다.

NEMC의 `hpid`와 HIRA의 암호화 요양기호가 서로 다른 코드이기 때문에 병원명과 주소 등을 이용해 대응 관계를 확인하고 있다.

동일 이름으로 같은 HIRA 식별자에 연결됐던 고양 명지병원(`A2100042`)과 제천 명지병원(`A2300009`)은 공식 HIRA 기관 페이지로 수동검증했다. 현재 전문의 수는 각각 12명과 7명이며 별도 식별자로 관리한다.

향후 자동매칭되지 않은 병원에 대한 추가 확인 및 수동 매핑이 완료되면 기존에 `원천데이터부족`이었던 지역 중 일부에서도 의료진부족점수와 최종 `regionRisk`를 추가로 계산할 수 있다.

HIRA 보완과 별개로, 행정안전부 2026년 7월 인구를 반영한 현재 산출물에서는 완료지역이 126개로 증가했다.

완료지역의 모집단이 바뀌면 본 보고서에서 사용한 다음 값도 다시 계산해야 한다.

```text
평균
중앙값
P20
P40
P60
P80
백분위
사용자용 단계 기준
```

따라서 본문 분위수 기준값은 **보고서 작성 당시 124개 완료지역에 대한 과거 기준**으로 관리한다. 현재 126개 완료지역의 구성점수 분위수는 보고서를 정식 재생성할 때 다시 계산해야 한다.

---

# 18. 카카오 도로거리 적용 이후 재분석

현재 프로젝트에서는 접근성 산정 방식을 기존 직선거리에서 실제 도로거리 기반으로 개선하는 작업을 진행하고 있다.

최종 구현에서는 카카오모빌리티 자동차 길찾기 API를 이용하여 실제 자동차 이동 경로의 거리를 반영할 예정이다.

현재 계획된 방식은 각 시군구 중심점에서 직선거리 기준으로 가까운 권역·지역응급의료센터를 후보로 선정한 뒤, 카카오 자동차 길찾기 API를 이용하여 실제 도로거리를 조회하고 가장 가까운 병원을 접근성 기준 병원으로 선택하는 방식이다.

---

## 18.1 왜 다시 분석해야 하는가

접근성 원천값이 바뀌면 단순히 접근성점수 하나만 바뀌는 것이 아니다.

접근성점수는 `regionRisk`의 30%를 차지하기 때문에 최종 위험도도 변할 수 있다.

또한 최종 위험도가 바뀌면 다음 분석 결과도 달라질 수 있다.

```text
접근성 원천값 변경
        ↓
접근성점수 변경
        ↓
regionRisk 변경
        ↓
상관관계 변경
        ↓
회귀분석 변경
        ↓
K-Means 군집 결과 변경
        ↓
사용자 해석 기준 변경
```

따라서 카카오 도로거리 적용 후에는 분석 파이프라인을 다시 실행해야 한다.

---

## 18.2 재생성이 필요한 결과 파일

최종 카카오 도로거리 적용 이후 다음 결과를 다시 확인한다.

```text
accessibility_score.csv
region_risk_final.csv
correlation_matrix.csv
vif_result.csv
regression_result.csv
regression_metrics.json
cluster_result.csv
cluster_profile.csv
cluster_k_evaluation.csv
```

그리고 새롭게 생성된 `region_risk_final.csv`를 이용하여 본 보고서의 사용자 해석 기준도 다시 계산한다.

---

## 18.3 변경 전·후 분석 혼합 금지

직선거리 기반 분석 결과와 카카오 도로거리 기반 분석 결과를 최종 보고서에서 하나의 분석처럼 섞어서 사용해서는 안 된다.

예를 들어 `regionRisk`는 카카오 도로거리 기반으로 다시 계산했는데 `regression_result.csv`는 기존 `직선거리_km` 기반 결과를 그대로 사용한다면 서로 다른 버전의 분석을 동시에 해석하게 된다.

따라서 최종 대시보드 및 최종 분석보고서에서는 **동일한 데이터 파이프라인 실행 결과에서 생성된 파일만 사용한다.**

---

# 19. 본 분석의 한계

## 19.1 상대평가 기준

본 보고서의 구성점수 단계는 실제 의료정책상 공식 기준이 아니다.

현재 분석 완료지역의 데이터 분포를 이용한 상대평가이다.

따라서:

> 접근성점수 60점 이상이면 의료적으로 위험하다.

와 같은 절대적인 판단에는 사용할 수 없다.

정확한 표현은:

> 현재 분석 완료지역의 분포에서 접근성 취약도가 높은 수준이다.

이다.

---

## 19.2 실시간 병상 데이터

병상포화도는 API를 실행한 시점의 실시간 병상상태에 영향을 받는다.

따라서 특정 시점의 병상포화도만으로 해당 지역의 평상시 응급의료 상황 전체를 판단하기 어렵다.

향후 `bed_status_history.csv`에 병상 스냅샷을 지속적으로 축적하면:

- 시간대별 병상포화도
- 평일과 주말 차이
- 요일별 패턴
- 계절별 패턴

등을 추가로 분석할 수 있다.

---

## 19.3 원천데이터 결측

해당 과거 스냅샷에서는 전체 219개 지역 중 93개 지역에서 적어도 하나의 구성점수가 부족했다.

따라서 당시 완료지역 126개만을 이용한 분포도 대한민국 전체 지역의 완전한 분포라고 볼 수 없다.

HIRA 매칭 및 기타 원천데이터 보완 이후 반드시 기준을 다시 계산해야 한다.

---

# 20. 분석적 의의

본 분석을 통해 동일한 `regionRisk`라도 지역별 위험 구조가 서로 다를 수 있다는 점을 사용자에게 설명할 수 있다.

어떤 지역은:

```text
병상포화도 ↑
접근성 양호
의료진 충분
```

일 수 있고, 다른 지역은:

```text
병상포화도 보통
접근성 매우 취약
의료진 매우 부족
```

일 수 있다.

따라서 최종 서비스의 목적은 단순히 지역을 위험도 순으로 정렬하는 것이 아니라:

> **해당 지역이 왜 위험한지를 설명하는 것**

에 있다.

이를 위해 네 구성점수 각각에 대해 실제 데이터 분포를 기반으로 사용자 해석 기준을 정의하고, 지역별 주요 위험요인을 자동으로 판별하여 제공한다.

---

# 21. 최종 제안

최종 대시보드에서는 지역별 정보를 다음과 같은 구조로 제공한다.

### 1. 종합위험도

```text
regionRisk
38.10 / 100
```

### 2. 위험등급

```text
3등급 · 보통
```

### 3. 구성요소

```text
병상포화도        90.48 · 매우 높음
접근성             1.26 · 매우 양호
인구 대비 병상    25.63 · 보통
의료진 부족        6.19 · 매우 양호
```

### 4. 주요 위험요인

```text
병상포화
```

### 5. 사용자 설명

```text
현재 병상 포화 부담은 분석지역 중 매우 높은 수준입니다.
반면 응급의료기관 접근성과 의료진 확보 측면의 취약성은
상대적으로 낮게 나타났습니다.

따라서 이 지역은 구조적인 의료 접근성 문제보다
현재 병상 포화가 주요 위험요인으로 분석됩니다.
```

---

# 22. 결론

본 프로젝트에서 산출되는 0~100점의 수치는 그 자체만으로는 사용자가 의미를 직관적으로 이해하기 어렵다.

따라서 본 분석에서는 보고서 작성 당시 `region_risk_final.csv`에서 최종 위험도가 산출된 124개 지역의 실제 데이터 분포를 이용하여 병상포화도, 접근성, 인구 대비 병상, 의료진 부족의 상대적인 해석 기준을 정의하였다.

최종 서비스에서는 다음 구조를 기본 원칙으로 사용한다.

```text
원점수
  ↓
상대적 단계
  ↓
종합 위험등급
  ↓
주요 위험요인
  ↓
사용자용 설명
```

이를 통해 사용자는 특정 지역의 `regionRisk`가 몇 점인지 확인하는 것에 그치지 않고:

**왜 이러한 점수가 산출되었는지, 해당 지역에서 어떤 요소가 상대적으로 취약한지, 그 수치를 어떻게 해석해야 하는지**

까지 함께 확인할 수 있다.

행정안전부 2026년 7월 인구와 통계청 SGIS 기반 `admdongkor` `20260701` 경계는 현재 파이프라인에 반영되었다. 이 경계는 CC BY 4.0·공공누리 제1유형 조건의 웹 시각화용 `light` 단순화 자료이며 법적·측량용 공식 원본이 아니다. 향후 HIRA 결측 보완 또는 자동차 길찾기 기반 접근성 데이터가 반영되면 전체 분석 파이프라인을 재실행하고, 변경된 `region_risk_final.csv`를 기준으로 본 보고서의 분위수와 사용자 해석 기준을 다시 산출한다.
