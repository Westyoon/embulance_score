# 응급의료 지역 위험도 대시보드 — 화면 구성 보고서

작성 목적: 프론트엔드 화면 구성안을 팀원들과 공유하고, 각 요소가 어떤 데이터와 논리에 근거했는지 설명하기 위한 문서입니다. 아직 개발 착수 전 단계이며, 첨부된 목업(mockup) 파일을 함께 보면서 읽는 것을 권장합니다.

---

## 1. 전체 구조

대시보드는 **탭 2개**로 구성됩니다.

| 탭 | 목적 | 핵심 상호작용 |
|---|---|---|
| ① 메인 지도 | "지금 어디가 위험한가"를 한눈에 파악 | 지도를 확대/이동하며 지역을 클릭 → 상세 팝업 |
| ② 분석 대시보드 | "왜 위험한가", 전체 통계·패턴을 근거자료로 확인 | 랭킹, 클러스터, 상관관계, 회귀분석 등을 스크롤로 탐색 |

지도는 "탐색·의사결정" 화면이고, 분석 대시보드는 "근거·설명" 화면이라는 역할 분담입니다.

---

## 2. 탭 1 — 메인 지도

### 2.1 지도 본체

- **형태**: 실제 시군구 행정경계(KOSTAT 기준, 251개 지역)를 사용한 코로플레스(choropleth) 지도입니다. 처음 시안이었던 육각형 타일 배치는 실제 지도 형태로 교체했습니다.
- **조작**: 마우스 휠로 확대/축소, 드래그로 이동, 지역 클릭 시 팝업이 뜹니다. 별도의 확대 버튼(+/−)과 초기화 버튼도 우측 상단에 배치했습니다.
- **색상 기준**: 아래 3항에서 설명하는 파랑→빨강 5단계 스케일을 그대로 사용합니다.
- **미산출 지역**: 위험도가 계산되지 않은 지역(원천데이터부족)은 짙은 회색으로 구분해, "위험도가 낮다"와 "데이터가 없다"를 시각적으로 절대 혼동하지 않도록 했습니다. 이는 원본 분석 리포트에서 강조한 원칙(추정치로 대체하지 않는다)을 화면에도 그대로 반영한 것입니다.

### 2.2 지역 클릭 팝업

지역을 클릭하면 다음 정보가 순서대로 나옵니다.

1. **지역명 + 클러스터 라벨**: "접근성·의료진 복합취약형" / "접근성 양호·병상부담형" 중 하나가 배지로 표시됩니다.
2. **종합 위험도 점수 + 등급**: 예) "76.6 / 100점 · 매우높음 등급". 등급은 3항의 5단계 기준표를 그대로 적용합니다.
3. **4개 구성점수 막대**: 병상포화도(35%) · 접근성(30%) · 인구대비병상(20%) · 의료진부족(15%). 각 막대에 가중치를 함께 표기해, "왜 이 점수가 크게 반영됐는지"를 바로 알 수 있게 했습니다.
4. **한 줄 설명 문장**: 예) "울릉군의 종합 위험도는 59.0점(매우높음)입니다. 네 요인 중 접근성(100점)과 의료진부족(100점)이 가장 큰 영향을 주고 있어, 접근성·의료진 복합취약형 지역으로 분석됩니다." — 이 문장은 상위 2개 구성요소를 자동으로 골라 생성되며, 원본 리포트의 지역별 해설(7장 지역 사례 분석) 서술 방식을 그대로 따랐습니다.
5. **소속 의료기관 리스트**: 병원명, 등급, 실시간 병상 상태(여유/혼잡/포화/미갱신)를 색점으로 표시합니다.

> 회의에서 나온 "지역별로 왜 이 점수가 나왔는지 설명이 있어야 한다"는 의견을 이 팝업의 3~4번 항목으로 반영했습니다.

### 2.3 사이드 패널 (지도 오른쪽)

토글 버튼으로 두 가지 보기를 전환합니다.

- **핵심 지표 보기**: 평균 위험도 · 고위험 지역 수(50점 이상) KPI 카드 2개, 그리고 전국 위험도 상위 10개 지역 랭킹 리스트. 리스트 항목을 클릭해도 지도 팝업과 동일한 상세 정보가 뜹니다.
- **병원 리스트 보기**: 전체 의료기관을 병상 상태 색점과 함께 리스트로 보여줍니다. 지도에서 병원을 찾기보다 리스트로 스캔하고 싶은 사용자를 위한 대체 경로입니다.

---

## 3. 색상 및 등급 기준 (회의에서 가장 중요하게 지적된 부분)

### 3.1 위험도 색상 — 파랑(안전) → 빨강(위험)

기존에는 초록→빨강 스케일이었는데, "가장 안전한 지역은 파랑, 가장 위험한 지역은 빨강"이라는 요청에 따라 아래 5단계로 교체했습니다. **이 기준표는 지도, 랭킹 차트 등 위험도 색상이 쓰이는 모든 화면에 항상 함께 노출**됩니다. 즉 "이 색이 무슨 뜻인지" 사용자가 매번 유추할 필요가 없습니다.

| 등급 | 점수 범위 | 색상 |
|---|---|---|
| 매우낮음 | 0 ~ 20점 | 파랑 |
| 낮음 | 20 ~ 35점 | 하늘색 |
| 보통 | 35 ~ 50점 | 노랑 |
| 높음 | 50 ~ 65점 | 주황 |
| 매우높음 | 65점 초과 | 빨강 |
| (미산출) | 데이터 없음 | 회색 |

구간 경계값은 현재 확보된 경북 13개 지역 실측치(13.7~88.1점 분포)와 원본 리포트의 위험등급 구분 방식을 참고해 잠정적으로 설정한 값입니다. **전국 124개 지역 전체 분포가 확보되면 분위수(quantile) 기준으로 재조정하는 것을 권장**하며, 이 부분은 팀 논의가 필요한 지점입니다.

### 3.2 상관관계 색상 — 별도의 보라색 계열 사용

"0.8009 같은 수치를 그대로 보여주면 안 된다"는 지적에 따라, 분석 대시보드의 상관관계 표현 방식을 아래처럼 전면 수정했습니다.

- **문제였던 것**: 상관계수(–1~1)를 숫자로 그대로 노출 → 통계를 모르는 사용자는 0.80이 강한 건지 약한 건지 판단 불가.
- **수정 방향**: 절댓값 기준 5단계로 구간화하고, 숫자 대신 "매우 약함 / 약함 / 보통 / 강함 / 매우 강함" 라벨과 백분율(예: "80%대 일치")로 표시.
- **범례를 x축 자체로 배치**: 화면 상단에 5단계 색상 바를 가로로 배치해 이것이 곧 "척도(범례)" 역할을 하고, 그 아래 4개 요인(접근성점수·의료진부족점수·병상포화도점수·인구대비병상점수)의 막대가 이 척도 위 어디쯤 위치하는지 보여줍니다. 원본 데이터의 0.8009, 0.6731, 0.4465, 0.1008 네 값이 이 방식으로 전부 시각화됩니다.
- **문장형 해설 동반**: 각 막대 아래 "응급의료센터 접근이 어려운 지역일수록 종합위험도도 함께 높아지는 경향이 네 요인 중 가장 뚜렷합니다" 같은 자연어 해설을 붙여, 수치를 안 봐도 의미가 전달되도록 했습니다.
- 위험도 색상(파랑~빨강)과 헷갈리지 않도록, 상관관계는 의도적으로 **보라색 계열**을 사용했습니다. 같은 화면에 두 가지 색상 스케일이 있을 때 이를 구분하는 것이 중요합니다.

---

## 4. 탭 2 — 분석 대시보드

지도 화면이 "어디가 위험한가"를 보여준다면, 이 화면은 "왜 그런가"에 대한 근거를 모아 놓은 화면입니다. 위에서 아래로 다음 순서로 배치했습니다.

### 4.1 상단 KPI 카드 4개
평균 위험도, 고위험 지역 수(50점 이상), 위험도 산출 완료/전체 지역 수(예: 124/219), 데이터 기준 시각. 대시보드에 들어오자마자 전체 그림을 파악하기 위한 요약 지표입니다.

### 4.2 지역별 위험도 랭킹 (상위 10)
가로 막대그래프이며, 막대 색상도 3.1의 5단계 기준을 그대로 사용합니다. 차트 바로 아래 기준표(범례)를 함께 배치해, 이 화면만 보고도 "왜 이 지역이 빨간색인지" 바로 알 수 있게 했습니다.

### 4.3 클러스터 프로필 비교 (레이더 차트)
K-Means 군집분석 결과(k=2)를 레이더 차트로 시각화했습니다. "접근성·의료진 복합취약형"(55개 지역, 평균 47.94점)과 "접근성 양호·병상부담형"(69개 지역, 평균 25.95점) 두 유형이 4개 구성요소에서 어떻게 다른지 한눈에 비교할 수 있습니다.

### 4.4 위험도에 영향을 주는 요인 순위 (구 상관관계 히트맵)
3.2에서 설명한 방식으로 재구성한 패널입니다. 기존 5×5 상관행렬 히트맵은 통계 지식이 없으면 해석이 어려워 폐기했고, "종합위험도와 각 구성요소의 관계"만 추려서 등급+문장으로 보여주는 방식으로 단순화했습니다.

### 4.5 지역별 위험 요인 분포 (버블차트)
x축 접근성점수, y축 의료진부족점수, 버블 크기는 병상포화도를 나타냅니다. 클러스터별로 색상을 다르게 표시해, 두 취약 유형이 실제로 어느 위치에 분포하는지 시각적으로 확인할 수 있습니다.

### 4.6 회귀분석 결과
- **회귀계수 막대**: 포화율, 직선거리, 인구대비병상비율, 전문의부족비율 네 원천변수의 계수를 보여줍니다. 인구대비병상비율 계수(0.000595)는 단위 스케일이 달라 절대값 비교가 어렵다는 주석을 함께 달았습니다.
- **실제값 vs 예측값 산점도**: R²=0.833, MAE=2.70을 함께 표기해 모델이 얼마나 잘 맞는지 보여줍니다. 대각선 기준선을 그려 예측이 실제값에서 얼마나 벗어나는지 직관적으로 확인할 수 있습니다.
- ⚠️ 현재 이 회귀 결과는 원본 리포트에 따르면 접근성 변수가 **직선거리 기준**으로 계산된 결과입니다. 카카오 도로거리 기반으로 접근성점수가 재계산되면 이 패널도 함께 갱신되어야 한다는 점을 화면 하단 주석으로 남겨둘 필요가 있습니다.

---

## 5. 데이터 출처 매핑

| 화면 요소 | 데이터 소스 |
|---|---|
| 지도 경계 | KOSTAT 시군구 행정경계 (별도 확보한 GeoJSON, 팀 파이프라인 산출물 아님) |
| 지역별 위험도·구성점수 | `region_risk_final.csv` |
| 클러스터 라벨/프로필 | `cluster_result.csv`, `cluster_profile.csv` |
| 상관관계 순위 패널 | `correlation_matrix.csv` |
| 회귀계수·산점도 | `regression_result.csv`, `regression_metrics.json` |
| 병원 리스트·병상 상태 | `hospital_master.csv`, `bed_status.csv` |

현재 목업에서 실측치를 그대로 사용한 지역은 리포트 8.1절에 표로 제시된 **경북 13개 지역**뿐이며, 나머지 지역은 "군 지역은 취약 패턴, 도시 구는 양호 패턴"이라는 실제 분석 결과의 경향성을 반영한 **목데이터**입니다. 팝업에는 목데이터인 지역에 "목데이터" 배지를 표시해 구분해 두었습니다.

---

## 6. 팀 논의가 필요한 항목

1. **위험도 5단계 구간 경계값**: 지금은 잠정치입니다. 전국 124개 지역 실측 분포가 나오면 분위수 기준으로 재산정할지, 지금처럼 절대점수 기준으로 고정할지 결정이 필요합니다.
2. **직선거리 → 카카오 도로거리 반영 여부**: 회귀분석 패널이 아직 구버전 접근성 변수를 쓰고 있어, 최종 데이터 파이프라인 갱신 시 함께 업데이트해야 합니다.
3. **지도 확대/이동 성능**: 현재는 251개 지역 경계를 클라이언트에서 직접 렌더링합니다. 실제 서비스에서는 초기 로딩 최적화(경계 단순화, 지연 로딩 등)를 검토해야 합니다.
4. **버블차트 대상 확대**: 지금은 실측치가 있는 경북 13개 지역만 표시됩니다. 전국 데이터가 준비되면 전체 지역으로 확장할지 결정이 필요합니다.

---

첨부: `emergency_risk_dashboard_mockup.jsx` (인터랙티브 목업, 브라우저에서 직접 확인 가능)
