# 원천 결측 복구 업무 인수인계

이 문서는 원천 데이터 결측 복구를 여러 담당자에게 업무 티켓으로 나누어 전달하기 위한 실행 문서입니다. 변동 수치는 고정 문서보다 [운영 `/api/health`](https://emergency-dashboard-production-e303.up.railway.app/api/health)와 해당 generation의 `missingness_followup.csv`를 우선합니다.

## 1. 목표와 현재 기준

목표는 전체 병원 결측을 임의로 채우는 것이 아니라, 공식 원천에서 유효한 값을 다시 받아 위험도 미산출 지역을 줄이는 것입니다.

- 점검 시각: 2026-09-04 03:31 KST
- 데이터 기준시각: 2026-09-04 01:03 KST
- NEMC 모집단: 534개 병원, 219개 지역
- 유효 병상: 410개 병원
- 최근 계산 위험도: 197개 지역
- 위험도 미산출: 22개 지역
- 병상 결측: 124개 병원
- HIRA 수동 검토: 2개 병원

`197개`는 해당 generation에서 계산된 지역 수입니다. 현재 화면에 표시 가능한 수는 병상 유효기간에 따라 계속 달라지므로 작업 시작·종료 시 `/api/health`의 `completeRegions`, `expiredScoreRegions`, `dataStale`를 따로 기록합니다.

| 원천 문제 | 전체 | 22개 미산출 지역에 직접 영향 | 업무 티켓 |
|---|---:|---:|---|
| NEMC 응답에 병원 없음 | 92곳 | 16곳 | `MISS-NEMC-01` |
| 기준시각 12시간 초과 | 25곳 | 6곳 | `MISS-NEMC-02` |
| 가용병상 음수 | 5곳 | 0곳 | `MISS-NEMC-03` |
| 총병상 누락 | 2곳 | 2곳 | `MISS-NEMC-04` |
| HIRA 후보 확정 보류 | 2곳 | 현재 지역 점수 영향 없음 | `MISS-HIRA-01` |
| NEMC 모집단 534→533 변화 | 1곳 | 전체 갱신 승격 차단 | `MISS-POP-01` |

## 2. 업무 배정표

담당자와 목표일만 채운 뒤 GitHub Issue 또는 협업 도구에 그대로 옮깁니다.

표의 P0/P1은 **인수인계 운영 우선순위**입니다. 생성 파일의 원천 품질 우선순위와 다를 수 있으며, HIRA 2곳은 `missingness_followup.csv`에서는 P2지만 전체 갱신 안정화를 위해 이 문서에서 P1로 올렸습니다.

| 티켓 | 우선순위 | 권장 담당 | 담당자 | 목표일 | 선행조건 | 완료 판단 |
|---|---|---|---|---|---|---|
| `MISS-NEMC-01` 무응답 16곳 | P0 | 원천 수집 담당 | `@________` | `____-__-__` | 없음 | 16곳 모두 복구 또는 공식 상태·문의번호 기록 |
| `MISS-NEMC-02` 만료 6곳 | P0 | 원천 수집 담당 | `@________` | `____-__-__` | 없음 | 최신 `hvidate` 수신 또는 원천기관 에스컬레이션 |
| `MISS-NEMC-04` 총병상 2곳 | P0 | 원천 수집 담당 | `@________` | `____-__-__` | 없음 | 유효 `hvs01` 수신 또는 공식 상태 기록 |
| `MISS-NEMC-03` 음수 5곳 | P1 | 데이터 품질 담당 | `@________` | `____-__-__` | P0 이후 가능 | 정상 `hvec` 수신 또는 원천 답변 기록 |
| `MISS-HIRA-01` 수동 검증 2곳 | P1 | HIRA 검증 담당 | `@________` | `____-__-__` | 공식 상세 조회 | 근거 있는 override/exclusion과 1:1 검증 통과 |
| `MISS-POP-01` 모집단 변화 1곳 | P0 | 데이터 책임자 | `@________` | `____-__-__` | 공식 운영 상태 확인 | 유지/제외 결정과 근거 기록 |
| `MISS-OPS-02` 실패 증거·재시도 개선 | P0 | 백엔드·운영 담당 | `@________` | `____-__-__` | 없음 | 실패 대상을 보존하고 동일 실패 반복 호출 차단 |
| `MISS-OPS-01` 재수집·승격 검증 | P0 | 백엔드·운영 담당 | `@________` | `____-__-__` | 위 티켓 결과 | `beds`/`full` 성공 및 전후 수치 보고 |

## 3. 모든 담당자가 지킬 규칙

1. `0`, 과거값 복사, 평균값, HIRA 일반 병상으로 NEMC 실시간 병상 결측을 대체하지 않습니다.
2. 생성 산출물인 `bed_status.csv`, `hira_doctor_matches.csv`, `region_risk_final.csv`를 직접 수정하지 않습니다.
3. 수동 판단은 공식 URL, 확인일, 판단 사유가 있는 override/exclusion 입력에만 기록합니다.
4. API 키가 포함된 요청 URL은 이슈·메신저·스크린샷에 붙이지 않습니다. `serviceKey`와 토큰은 `<REDACTED>`로 치환합니다.
5. `POST /api/ops/refresh`의 HTTP 202는 완료가 아니라 접수입니다. `/api/health`에서 최종 성공과 `dataVersion` 변경을 확인합니다.
6. 429·할당량 초과·동일 실패 상태에서 수동 재실행을 반복하지 않습니다.

증빙 파일명은 `{티켓ID}_{기관코드}_{요청시각KST}.{json|xml|txt}` 형식을 사용합니다. 원응답은 키를 제거한 뒤 팀의 비공개 증빙 저장소에 보관하고, 공개 GitHub Issue에는 요약·문의번호·접근 가능한 근거 링크만 남깁니다.

## 4. 복사해서 사용할 업무 티켓

### MISS-NEMC-01 — 위험도 미산출 지역의 NEMC 무응답 16곳 확인

- 담당자: `@________`
- 목표일: `____-__-__`
- 목적: NEMC 지역별 실시간 병상 응답에서 기관코드 `hpid`가 빠지는 원인을 확정하고 복구 또는 공식 에스컬레이션합니다.

대상:

| 기관코드 | 병원 | 지역 |
|---|---|---|
| `A2100135` | 동두천중앙성모병원 | 경기 동두천시 |
| `A2800826` | 하동중앙의원 | 경남 하동군 |
| `A2800138` | 영동병원 | 경남 함안군 |
| `A2700071` | 봉화해성병원 | 경북 봉화군 |
| `A2700073` | 영덕아산병원 | 경북 영덕군 |
| `A2700026` | 영양병원 | 경북 영양군 |
| `A2700066` | 왜관병원 | 경북 칠곡군 |
| `A1303449` | 행복한병원 | 대구 달성군 |
| `A1200081` | 세웅병원 | 부산 금정구 |
| `A1200089` | 큐병원 | 부산 사하구 |
| `A1100172` | 서울현대병원 | 서울 강북구 |
| `A1121842` | 강북으뜸병원 | 서울 강북구 |
| `A1100063` | 신촌연세병원 | 서울 마포구 |
| `A1403699` | 영종국제병원 | 인천 영종구 |
| `A2600090` | 함평성심병원 | 전남광주통합특별시 함평군 |
| `A2400019` | 새금산병원 | 충남 금산군 |

작업:

1. NEMC `getEmrrmRltmUsefulSckbdInfoInqire` 원응답과 정기 `beds` 실행 3회에서 동일 기관이 계속 누락되는지 확인합니다.
2. 각 호출의 요청 시각, `STAGE1`, `STAGE2`, 결과코드, 해당 `hpid` 존재 여부를 기록합니다.
3. NEMC 기관 마스터에서 운영 여부, 기관명 변경, 지역 변경, 응급실 운영 종료 여부를 확인합니다.
4. 운영 중인데 계속 누락되면 NEMC 또는 공공데이터포털에 원천 전송 확인을 요청합니다.

산출물:

- 기관별 `복구 / 일시 누락 / 운영 종료 의심 / 원천 문의 중` 상태
- 요청 시각과 키를 제거한 원응답
- 공식 문의번호 또는 근거 URL

완료 조건:

- 16곳 모두 상태와 근거가 기록되어야 합니다.
- 복구된 병원은 수동 CSV 수정 없이 `beds` 파이프라인으로 유입되어야 합니다.

### MISS-NEMC-02 — 기준시각 만료 6곳 갱신 확인

- 담당자: `@________`
- 목표일: `____-__-__`
- 목적: 12시간을 초과한 `hvidate`가 원천에서 갱신되도록 확인합니다.

대상:

| 기관코드 | 병원 | 마지막 확인 기준시각 |
|---|---|---|
| `A2100282` | 에이치제이매그놀리아국제병원 | 2018-01-02 |
| `A2100134` | 양주예쓰병원 | 2020-11-02 |
| `A2700030` | 성주병원 | 2024-01-19 |
| `A1100030` | 대한병원 | 2021-06-02 |
| `A2300017` | 보은한양병원 | 2026-06-09 |
| `A2300015` | 제일조은병원 | 2021-05-11 |

작업:

1. 최신 원응답의 `hvidate`와 수집시각 차이를 확인합니다.
2. NEMC 또는 병원에 실시간 병상 전송 여부를 확인합니다.
3. 새 `hvidate`가 수신되면 다음 `beds` 실행에서 유효 처리되는지 확인합니다.

완료 조건:

- `hvidate`가 수집시각 기준 12시간 이내로 갱신되거나, 원천기관 문의 결과와 후속일이 기록되어야 합니다.

### MISS-NEMC-03 — 음수 가용병상 5곳 검증

- 담당자: `@________`
- 목표일: `____-__-__`
- 목적: 음수 `hvec`를 임의 보정하지 않고 다음 정상 관측 또는 원천 답변을 확보합니다.

대상:

- `A1100006` 강북삼성병원
- `A1100010` 삼성서울병원
- `A1100012` 서울성모병원
- `A1100017` 서울대학교병원
- `A2100001` 분당서울대학교병원

완료 조건:

- `hvec >= 0`인 새 원응답이 수신되거나, 음수값 의미와 처리 방법에 대한 공식 답변이 기록되어야 합니다.

### MISS-NEMC-04 — 총병상 누락 2곳 확인

- 담당자: `@________`
- 목표일: `____-__-__`
- 목적: NEMC 응급실 기준 병상 `hvs01`을 확보합니다.

대상:

- `A2700063` 영남제일병원 — 경북 의성군
- `A2800094` 남해병원 — 경남 남해군

작업:

1. 최신 NEMC 원응답에 `hvs01`이 없거나 0 이하인지 확인합니다.
2. HIRA 일반 병상 수로 대체하지 않습니다.
3. NEMC/병원에 응급실 기준 병상 등록을 요청합니다.

완료 조건:

- `hvs01 > 0`, `hvec >= 0`, 최신 `hvidate`를 동시에 만족하는 원응답이 수신되거나 공식 후속일이 기록되어야 합니다.

### MISS-HIRA-01 — HIRA 수동 검증 2곳

- 담당자: `@________`
- 목표일: `____-__-__`
- 대상: `A2100283` 일산복음병원(경기 고양시), `A2300039` 마이크로병원(충북 청주시)
- 목적: 병원명만으로 추정하지 않고 HIRA 요양기호를 1:1로 확정하거나 공식 근거로 제외합니다.

작업:

1. HIRA 공식 상세에서 기관명, 주소, 전화, 기관구분, 암호화 요양기호를 확인합니다.
2. NEMC 기관과 같은 기관인지 판단합니다.
3. 공식 URL, 확인일, 판단 메모를 `data/hira_match_overrides.csv` 또는 `data/hira_match_exclusions.csv`에 기록합니다.
4. 동일 HIRA 식별자가 다른 NEMC 병원에 배정되지 않는지 검사합니다.

완료 조건:

- 두 기관 모두 근거가 있는 확정 또는 제외 상태여야 합니다.
- `full` 실행에서 HIRA 1:1 계약과 지역 80% 품질 기준을 통과해야 합니다.

### MISS-POP-01 — 강릉고려병원 모집단 변화 판단

- 담당자: `@________`
- 목표일: `____-__-__`
- 대상: `A2200010` 의산의료재단강릉고려병원
- 목적: 최신 NEMC 기관 목록에서 모집단이 534→533으로 감소한 것이 실제 변경인지 일시 누락인지 판단합니다.

재현 기준은 `/api/health`의 `pipeline.lastFullFailureAt`과 해당 시각의 Railway `full` 로그입니다. 2026-09-04 12:23 KST 실행은 모집단 감소 안전장치에서 실패했습니다. 현재 별도 run ID와 실패 artifact가 남지 않는 문제는 `MISS-OPS-02`가 해결합니다.

작업:

1. NEMC 공식 기관정보에서 폐업, 응급실 운영 종료, 기관코드 변경 여부를 확인합니다.
2. 실제 변경이면 공식 근거와 함께 모집단 변경을 승인합니다.
3. 일시 누락이면 기존 534개 모집단을 유지하고 원천 회복을 기다립니다.
4. 판단 전 `full` 자동 실패 재시도가 API 호출량을 낭비하지 않도록 운영 담당자에게 알립니다.

완료 조건:

- `유지` 또는 `제외/변경` 결정, 공식 근거 URL, 확인일, 승인자가 기록되어야 합니다.

### MISS-OPS-02 — 실패 증거 보존과 반복 재시도 방지

- 담당자: `@________`
- 목표일: `____-__-__`
- 목적: 실패한 `full` staging이 폐기된 뒤에도 원인 기관을 식별할 수 있게 하고, 같은 실패로 외부 API를 반복 호출하지 않게 합니다.

작업:

1. 실패 단계, 대상 기관코드, 직전·현재 모집단/매칭 수와 원인코드를 키를 제거한 진단 파일에 보존합니다.
2. 실패 fingerprint가 같고 입력·원천이 바뀌지 않았다면 `full` 자동 재시도를 억제하거나 24시간 이상으로 늦춥니다.
3. `beds` 8시간 갱신은 그대로 유지합니다.
4. `/api/health`에서 마지막 성공, 실패 단계, 실패 대상 수와 다음 재시도 시점을 확인할 수 있게 합니다.

산출물:

- 실패 review artifact
- 변경된 재시도 정책과 운영 runbook
- 키가 제거된 Railway 로그 예시

완료 조건:

- 실패 후에도 원인 기관을 기관코드로 식별할 수 있어야 합니다.
- 같은 실패로 `full`이 24시간 안에 반복 호출되지 않아야 합니다.
- `beds` 정기 갱신은 계속 성공해야 합니다.

### MISS-OPS-01 — 재수집과 운영 승격 검증

- 담당자: `@________`
- 목표일: `____-__-__`
- 선행조건: 원천 관련 티켓 결과가 준비되어야 합니다.

작업:

1. 병상 원천 복구분은 인증된 운영 API로 `beds`를 한 번 실행합니다.
2. HIRA 또는 모집단 관리 입력이 변경됐다면 모든 근거를 반영한 뒤 `full`을 한 번 실행합니다.
3. `/api/health`에서 `pipeline.state`, `lastBedsSuccessAt` 또는 `lastFullSuccessAt`, `dataVersion`을 확인합니다.
4. `/api/dashboard`에서 `currentRiskAvailable`, 위험도 산출 지역 수, 결측 지역 수를 확인합니다.
5. 해당 generation의 `missingness_followup.csv` 원인별 합계가 병원 수와 일치하는지 검증합니다.

완료 보고 양식:

```text
[MISS-OPS-01 완료 보고]
- 실행 모드: beds / full
- 실행 시작·종료 시각:
- dataVersion 전 → 후:
- 유효 병원 전 → 후:
- 위험도 산출 지역 전 → 후:
- 결측 지역 전 → 후:
- 남은 원인코드별 건수:
- 실패 또는 보류 항목:
- 관련 티켓/공식 근거:
```

완료 조건:

- 파이프라인이 최종 성공해야 합니다.
- 기존 정상 generation을 보존하면서 새 generation이 데이터 계약을 통과해야 합니다.
- 숫자가 개선되지 않아도 원천이 여전히 미전송임을 증명했다면 해당 원천 티켓은 `에스컬레이션 완료`로 넘길 수 있습니다.

## 5. 원천기관 문의 템플릿

```text
제목: NEMC 실시간 병상 API 기관 데이터 확인 요청 — {기관코드} {병원명}

- 기관코드(hpid):
- 병원명 / 지역:
- 사용 서비스·오퍼레이션: NEMC 실시간 가용병상 정보
- 확인한 요청 시각(KST):
- 요청 지역(STAGE1/STAGE2):
- 증상: 기관 미반환 / hvidate 만료 / hvec 음수 / hvs01 누락
- 마지막 정상 원천시각과 값:
- 재현 횟수와 시각:
- 키를 제거한 응답 일부:
- 확인 요청사항:

※ 서비스 키와 운영 토큰은 첨부하지 않습니다.
```

## 6. 업무 종료 전 체크리스트

- [ ] P0 대상 24개 병원의 상태와 증빙이 모두 기록됐다.
- [ ] 원천 결측을 0 또는 추정값으로 채우지 않았다.
- [ ] 변경된 관리 입력에 공식 URL과 확인일이 있다.
- [ ] `beds` 또는 `full`을 필요한 횟수만 실행했다.
- [ ] `/api/health`의 최종 성공과 `dataVersion` 변경을 확인했다.
- [ ] 미산출 22개 지역의 전후 변화를 기록했다.
- [ ] 남은 결측에는 담당자, 다음 확인일, 외부 문의번호가 있다.

권장 실행 순서는 `MISS-OPS-02` → `MISS-NEMC-01·02·04` 병렬 → `MISS-HIRA-01·MISS-POP-01` 병렬 → `MISS-OPS-01` → `MISS-NEMC-03`입니다.
