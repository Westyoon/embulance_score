# 전국 응급의료 지역 위험도 분석 보고서 — 과거 분석 스냅샷

> [!IMPORTANT]
> 이 보고서 본문의 통계·순위·회귀·군집 수치는 갱신 전 데이터(`KOSIS 2024` 인구, 최종 산출 124개/결측 95개 지역)로 작성한 보관용 스냅샷이다. 현재 검증 산출물은 NEMC 534개 기관·219개 지역을 기준으로 행정안전부 2026년 7월 인구를 전 지역에 연결했으며, `regionRisk`는 196개 지역에서 산출되고 23개 지역은 `원천데이터부족`이다. 현재 수치는 `data/region_risk_final.csv`와 `README.md`를 우선한다.
>
> 현행 위험등급은 `20 이하=매우낮음`, `20 초과 35 이하=낮음`, `35 초과 50 이하=보통`, `50 초과 65 이하=높음`, `65 초과=매우높음`이다. 본문에 남아 있는 과거 20점 단위 등급이나 당시 사례 등급은 현재 서비스 기준으로 사용하지 않는다.
>
> 현행 PART 4 회귀는 전문의 0명 지역도 포함하도록 의료진 변수에 `의료진부족점수`를 사용한다. 완료지역 196개 전체가 분석되며 R²는 `0.8613975747`, MAE는 `3.0653038392`이다. 본문의 과거 79개 지역 회귀 결과는 현행 결과가 아니다.
>
> HIRA 중복 식별자 2건은 공식 HIRA 기관 페이지로 수동검증해 고양 명지병원(`A2100042`) 12명, 제천 명지병원(`A2300009`) 7명으로 분리했다. 새강병원(`A2502742`) 좌표 1건도 검증 자료로 보완되어 현재 NEMC 병원 좌표 결측은 0개다.
>
> 지도 경계는 통계청 SGIS 경계를 `admdongkor`가 가공한 `20260701` `light` 단순화 자료 256개로 교체되었다. 데이터 라이선스는 CC BY 4.0과 공공누리 제1유형이며, 법적·측량용 공식 원본이 아니다. 206개는 직접 연결되고, NEMC 상위 시 13개의 집계값이 일반구 경계 39개에 연결된다. 최신 경계 중 NEMC 기관이 없는 11개 지역은 `미산출`로 표시한다.

> 전국 응급의료기관의 실시간 병상 현황, 의료 접근성, 인구 대비 병상 수준, 응급의학과 전문의 현황을 통합하여 지역별 응급의료 위험도를 분석한 결과를 정리한다.

---

## 1. 분석 개요

본 프로젝트는 전국 응급의료기관 데이터를 활용하여 시군구별 응급의료 위험도를 정량적으로 평가하는 것을 목적으로 한다.

단순히 병원이 많고 적음을 기준으로 지역의 응급의료 수준을 판단하지 않고, 다음 네 가지 요소를 함께 고려하였다.

1. **병상포화도**: 현재 응급실 병상이 얼마나 사용되고 있는가
2. **접근성**: 해당 지역에서 응급의료센터까지 접근하기 얼마나 어려운가
3. **인구 대비 병상**: 지역 인구에 비해 응급실 병상이 충분한가
4. **의료진 부족**: 응급실 병상 규모에 비해 응급의학과 전문의가 충분한가

이를 통해 각 시군구의 최종 응급의료 위험도인 `regionRisk`를 산출하였다.

본 보고서에서는 단순히 위험도 순위를 제시하는 데 그치지 않고, 각 분석 결과가 **어떤 데이터 파일의 어떤 변수를 이용하여 산출되었는지**, 그리고 그 결과를 **어떻게 해석할 수 있는지**를 함께 설명한다.

---

# 2. 분석에 사용한 주요 파일

## 2.1 `data/region_risk_final.csv`

[`data/region_risk_final.csv`](data/region_risk_final.csv)는 본 프로젝트에서 가장 핵심적인 최종 결과 파일이다.

시군구별 네 가지 구성점수와 최종 `regionRisk`, 위험등급 및 위험도 산출 여부가 포함되어 있다.

| 변수           | 설명                            |
| ------------ | ----------------------------- |
| `시군구코드`      | 시도와 시군구를 결합한 지역 식별값           |
| `시군구명`       | 시군구명                          |
| `병상포화도점수`    | 병상 포화 정도를 0~100점으로 변환한 값      |
| `병상데이터기관수`   | 해당 지역 병상 분석에 사용된 기관 수         |
| `접근성점수`      | 응급의료기관 접근 취약성을 0~100점으로 변환한 값 |
| `인구대비병상점수`   | 지역 인구에 비해 병상이 부족한 정도          |
| `의료진부족점수`    | 병상 대비 응급의학과 전문의가 부족한 정도       |
| `완성항목수`      | 네 구성점수 중 실제 확보된 항목 수          |
| `regionRisk` | 최종 지역 응급의료 위험도                |
| `위험등급`       | 위험도를 단계화한 값                   |
| `산출상태`       | `완료` 또는 `원천데이터부족`             |

네 구성점수가 모두 존재하는 지역만 `regionRisk`를 산출한다.

예를 들어 경상북도 고령군은 네 점수가 모두 존재하여 `regionRisk=49.22`가 산출되어 있지만, 경산시·경주시·구미시 등은 일부 데이터가 부족하여 최종 위험도가 산출되지 않았다.

---

## 2.2 `data/correlation_matrix.csv`

[`data/correlation_matrix.csv`](data/correlation_matrix.csv)는 네 구성점수와 최종 `regionRisk` 사이의 피어슨 상관계수를 계산한 결과이다.

주요 분석값은 다음과 같다.

| 변수       | `regionRisk`와의 상관계수 |
| -------- | ------------------: |
| 병상포화도점수  |              0.4465 |
| 접근성점수    |          **0.8009** |
| 인구대비병상점수 |              0.1008 |
| 의료진부족점수  |          **0.6731** |

또한 구성점수 간 관계 중에서는 다음 값이 확인되었다.

| 변수 조합              |       상관계수 |
| ------------------ | ---------: |
| 접근성점수 ↔ 의료진부족점수    | **0.7837** |
| 접근성점수 ↔ 인구대비병상점수   |    -0.2391 |
| 의료진부족점수 ↔ 인구대비병상점수 |    -0.3277 |
| 병상포화도점수 ↔ 접근성점수    |    -0.0475 |

이 파일은 **어떤 구성요소가 최종 위험도와 함께 움직이는지**, 그리고 **취약요인끼리 어떤 관계를 가지는지**를 분석하는 데 사용하였다.

---

## 2.3 `data/regression_result.csv`

[`data/regression_result.csv`](data/regression_result.csv)는 정규화된 구성점수 대신 원천값을 독립변수로 사용하여 `regionRisk`를 설명하는 선형회귀분석 결과이다.

보고서 작성 당시 파일에 기록된 회귀계수는 다음과 같다.

| 원천변수          |     회귀계수 |
| ------------- | -------: |
| `포화율_원천`      |   0.3334 |
| `직선거리_km`     |   0.6282 |
| `인구대비병상비율`    | 0.000595 |
| `병상대비전문의부족비율` |   0.8945 |

보고서 작성 당시 회귀분석 대상은 79개 지역이었으며, 모델 성능은 다음과 같았다.

* R² = **0.833**
* MAE = **2.70**

단, 현재 파일에는 접근성 변수가 `직선거리_km`로 기록되어 있으므로, 카카오 자동차 도로거리 기반 접근성으로 변경한 이후 생성된 결과인지 최종 확인이 필요하다.

---

## 2.4 `data/cluster_result.csv`

[`data/cluster_result.csv`](data/cluster_result.csv)는 최종 위험도가 존재하는 지역을 대상으로 K-Means 군집분석을 실시한 결과이다.

주요 변수는 다음과 같다.

| 변수           | 설명              |
| ------------ | --------------- |
| `시군구코드`      | 지역 식별값          |
| `시군구명`       | 지역명             |
| `병상포화도점수`    | 병상포화 취약도        |
| `접근성점수`      | 접근성 취약도         |
| `인구대비병상점수`   | 인구 대비 병상 취약도    |
| `의료진부족점수`    | 의료진 부족 취약도      |
| `regionRisk` | 최종 위험도          |
| `클러스터`       | K-Means로 분류된 군집 |

총 124개 지역이 군집분석에 포함되었다.

---

## 2.5 `data/cluster_profile.csv`

[`data/cluster_profile.csv`](data/cluster_profile.csv)는 `cluster_result.csv`의 개별 지역 결과를 군집별 평균으로 요약한 파일이다.

| 클러스터 | 병상포화도 |   접근성 | 인구대비병상 |     의료진부족 | 평균 regionRisk | 지역 수 |
| ---: | ----: | ----: | -----: | --------: | ------------: | ---: |
|    0 | 35.54 | 58.03 |  19.38 | **94.79** |     **47.94** |   55 |
|    1 | 42.98 |  5.13 |  38.59 | **11.00** |     **25.95** |   69 |

이 파일을 이용하여 각 군집의 대표적인 특성을 해석하였다.

---

# 3. 지역 위험도 산출 방법

## 3.1 최종 위험도 공식

최종 지역 위험도는 다음과 같이 계산한다.

```text
regionRisk =
    0.35 × 병상포화도점수
  + 0.30 × 접근성점수
  + 0.20 × 인구대비병상점수
  + 0.15 × 의료진부족점수
```

즉, 가중치는 다음과 같다.

| 구성요소     | 가중치 |
| -------- | --: |
| 병상포화도    | 35% |
| 접근성      | 30% |
| 인구 대비 병상 | 20% |
| 의료진 부족   | 15% |

모든 구성점수는 0~100점 범위로 정규화된다.

따라서 `regionRisk` 역시 0~100 범위에서 산출된다.

---

## 3.2 실제 계산 사례: 철원군

`region_risk_final.csv`에서 철원군의 구성점수는 다음과 같다.

| 구성요소     |     점수 |
| -------- | -----: |
| 병상포화도점수  | 100.00 |
| 접근성점수    |  69.83 |
| 인구대비병상점수 |  28.02 |
| 의료진부족점수  | 100.00 |

이를 위험도 공식에 적용하면 다음과 같다.

```text
병상포화도 기여분
= 100 × 0.35
= 35.00

접근성 기여분
= 69.8308 × 0.30
= 20.9492

인구 대비 병상 기여분
= 28.0181 × 0.20
= 5.6036

의료진 부족 기여분
= 100 × 0.15
= 15.00
```

따라서

```text
regionRisk
= 35.00 + 20.9492 + 5.6036 + 15.00
= 76.5528
```

실제 `region_risk_final.csv`에 저장된 철원군의 `regionRisk`는 약 **76.55점**이다.

따라서 철원군의 높은 위험도는 단순히 병상 문제 하나 때문이 아니라,

* 높은 병상포화
* 낮은 응급의료 접근성
* 심각한 의료진 부족

이 동시에 나타난 **복합취약 결과**라고 해석할 수 있다.

---

## 3.3 결측값 처리

최종 `regionRisk`는 네 구성점수가 모두 존재하는 지역에서만 계산한다.

예를 들어 일부 지역은 다음과 같이 세 개의 값만 존재한다.

```text
병상포화도점수      존재
접근성점수          존재
인구대비병상점수    존재
의료진부족점수      결측
```

이 경우 나머지 세 점수만 이용하여 위험도를 임의로 계산하지 않는다.

`regionRisk`를 결측으로 유지하고 `산출상태`를 `원천데이터부족`으로 기록한다.

이러한 처리 방식은 데이터가 존재하지 않는 것을 0점으로 잘못 해석하는 문제를 방지한다.

특히 `의료진부족점수`가 결측인 것은 **전문의 부족이 없다는 의미가 아니다.**

해당 지역의 HIRA 의료기관 매칭이나 의료진 데이터가 위험도 산출 기준을 충족하지 못했다는 의미이다.

---

# 4. 상관관계 분석

## 4.1 분석 방법

상관관계 분석에는 `data/correlation_matrix.csv`를 사용하였다.

상관계수는 -1에서 1 사이의 값을 가진다.

* 1에 가까울수록 강한 양의 관계
* 0에 가까울수록 선형관계가 약함
* -1에 가까울수록 강한 음의 관계

다만 본 프로젝트에서 `regionRisk`는 네 구성점수를 직접 가중합하여 만든 값이다.

따라서 구성점수와 `regionRisk` 사이의 상관관계를 **독립적인 인과관계로 해석해서는 안 된다.**

---

# 5. 접근성점수와 최종 위험도

## 5.1 사용 데이터

`data/correlation_matrix.csv`에서 다음 위치를 확인하였다.

```text
행: 접근성점수
열: regionRisk
```

확인된 상관계수는 다음과 같다.

```text
r = 0.8009
```

네 구성요소 중 가장 높은 값이다.

---

## 5.2 결과 해석

0.8009는 강한 양의 상관관계를 의미한다.

즉, 현재 분석 대상 지역에서는 **접근성 취약점수가 높은 지역일수록 최종 `regionRisk` 역시 높은 경향**이 나타났다.

중요한 것은 `접근성점수`가 높을수록 접근성이 좋다는 뜻이 아니라는 것이다.

본 프로젝트의 점수체계에서는 점수가 높을수록 **취약성이 높다.**

따라서 결과는 다음과 같이 해석한다.

> 응급의료센터에 접근하기 어려운 지역일수록 최종 지역 응급의료 위험도 역시 높은 경향이 나타났다.

---

## 5.3 실제 지역 데이터와 비교

`cluster_result.csv`를 확인하면 접근성점수가 높은 지역 중 상당수가 높은 `regionRisk`를 보인다.

예를 들어 다음과 같다.

| 지역  |  접근성점수 | regionRisk |
| --- | -----: | ---------: |
| 통영시 |  91.01 |      88.11 |
| 거창군 | 100.00 |      80.19 |
| 합천군 | 100.00 |      78.87 |
| 진도군 |  93.72 |      73.96 |
| 울릉군 | 100.00 |      59.00 |
| 울진군 | 100.00 |      52.86 |

반대로 접근성점수가 낮은 지역은 상대적으로 낮은 위험도를 보이는 사례가 많다.

| 지역  | 접근성점수 | regionRisk |
| --- | ----: | ---------: |
| 안동시 |  1.89 |      13.67 |
| 문경시 |  0.00 |      15.17 |
| 김천시 |  1.90 |      20.23 |
| 포항시 |  3.98 |      20.83 |

---

## 5.4 해석 시 주의점

이를 근거로

> "접근성이 응급의료 위험의 원인임이 증명되었다."

라고 표현해서는 안 된다.

`regionRisk` 산식 자체에 접근성점수가 30% 포함되어 있기 때문이다.

따라서 보다 정확한 결론은 다음과 같다.

> 현재 설정한 위험도 산식과 분석 대상 지역의 점수 분포를 기준으로 볼 때, 접근성점수는 네 구성요소 중 최종 위험도와 가장 강하게 함께 변화하는 변수로 나타났다.

---

# 6. 의료진 부족과 최종 위험도

## 6.1 사용 데이터

`data/correlation_matrix.csv`에서

```text
행: 의료진부족점수
열: regionRisk
```

값을 확인하였다.

```text
r = 0.6731
```

이는 비교적 강한 양의 상관관계이다.

---

## 6.2 실제 사례

`cluster_result.csv`를 보면 다음 지역들은 의료진부족점수가 100점이며 최종 위험도 역시 높은 편이다.

| 지역  | 의료진부족점수 | regionRisk |
| --- | ------: | ---------: |
| 통영시 |     100 |      88.11 |
| 거창군 |     100 |      80.19 |
| 합천군 |     100 |      78.87 |
| 창녕군 |     100 |      78.24 |
| 철원군 |     100 |      76.55 |
| 진도군 |     100 |      73.96 |

따라서 현재 데이터에서는 전문의 부족 수준이 높은 지역에서 최종 위험도 역시 높은 경향이 확인된다.

---

# 7. 접근성과 의료진 부족의 동시 발생

## 7.1 사용 데이터

`data/correlation_matrix.csv`에서

```text
행: 접근성점수
열: 의료진부족점수
```

를 확인하였다.

결과는 다음과 같다.

```text
r = 0.7837
```

이는 강한 양의 상관관계이다.

---

## 7.2 의미

즉, 응급의료기관 접근성이 취약한 지역에서 의료진 부족 문제도 함께 나타나는 경향이 존재한다.

실제로 다음과 같은 지역에서 두 점수가 동시에 높다.

| 지역  |  접근성점수 | 의료진부족점수 |
| --- | -----: | ------: |
| 거창군 | 100.00 |     100 |
| 합천군 | 100.00 |     100 |
| 울릉군 | 100.00 |     100 |
| 완도군 | 100.00 |     100 |
| 진도군 |  93.72 |     100 |
| 통영시 |  91.01 |     100 |
| 철원군 |  69.83 |     100 |

따라서 지역 응급의료 취약성은 하나의 문제만 독립적으로 발생하기보다 **지리적 접근성 부족과 전문인력 부족이 동시에 나타나는 복합적인 형태**를 가질 가능성이 있다.

단, 현재 분석만으로 접근성이 낮기 때문에 의료진이 부족해졌는지 또는 의료진 부족 때문에 접근 가능한 의료기관이 적어진 것인지는 판단할 수 없다.

---

# 8. 병상포화도와 최종 위험도

## 8.1 사용 데이터

`data/correlation_matrix.csv`에서 확인한 값은 다음과 같다.

```text
병상포화도점수 ↔ regionRisk
r = 0.4465
```

중간 정도의 양의 상관관계이다.

---

## 8.2 특징

병상포화도는 `regionRisk` 계산에서 **35%로 가장 높은 가중치**를 가지고 있다.

그런데 실제 상관계수는 접근성점수의 0.8009보다 낮다.

이는 병상포화도가 높은 지역이라고 해서 항상 최종 위험도가 가장 높은 것은 아니기 때문이다.

---

## 8.3 서울 강남구 사례

`region_risk_final.csv`에서 강남구는 다음과 같다.

| 구성요소       |    점수 |
| ---------- | ----: |
| 병상포화도      | 90.48 |
| 접근성        |  1.26 |
| 인구대비병상     | 25.63 |
| 의료진 부족     |  6.19 |
| regionRisk | 38.10 |

병상포화도는 약 90점으로 매우 높다.

그러나 접근성과 의료진부족점수가 매우 낮기 때문에 최종 위험도는 38.10점으로 나타난다.

---

## 8.4 울릉군과 비교

울릉군은 다음과 같다.

| 구성요소       |  점수 |
| ---------- | --: |
| 병상포화도      |  40 |
| 접근성        | 100 |
| 인구대비병상     |   0 |
| 의료진 부족     | 100 |
| regionRisk |  59 |

울릉군의 병상포화도점수는 강남구보다 훨씬 낮다.

그러나 접근성과 의료진부족점수가 각각 100점이기 때문에 최종 위험도는 오히려 강남구보다 높다.

따라서 다음과 같이 해석할 수 있다.

> 높은 병상포화도는 지역 위험도를 높이는 중요한 요소이지만, 병상포화도 하나만으로 지역의 종합적인 응급의료 위험을 설명하기는 어렵다.

---

# 9. 인구 대비 병상과 최종 위험도

## 9.1 사용 데이터

`data/correlation_matrix.csv`에서

```text
인구대비병상점수 ↔ regionRisk
r = 0.1008
```

이 확인되었다.

네 구성요소 중 가장 낮은 상관계수이다.

---

## 9.2 해석

이는 현재 분석 대상 지역에서 **인구 대비 병상 부족 정도가 최종 위험도 순위와 강하게 일치하지 않았음**을 의미한다.

그러나 이것을

> "병상 수는 중요하지 않다."

라고 해석해서는 안 된다.

인구대비병상점수 역시 `regionRisk` 산식에 20% 반영되기 때문에 개별 지역의 위험도에는 영향을 준다.

---

## 9.3 서울 강서구 사례

강서구는 다음과 같다.

```text
병상포화도점수       10.00
접근성점수            1.52
인구대비병상점수    100.00
의료진부족점수        0.00

regionRisk           23.96
```

인구대비병상점수는 최고 수준인 100점이지만, 다른 구성요소가 낮기 때문에 최종 위험도는 약 24점이다.

---

## 9.4 통영시와 비교

통영시는

```text
병상포화도점수      100.00
접근성점수           91.01
인구대비병상점수     54.05
의료진부족점수      100.00

regionRisk           88.11
```

이다.

인구대비병상점수가 100점은 아니지만 다른 취약요인이 동시에 높아 최종 위험도가 매우 높다.

따라서 현재 결과는 다음을 보여준다.

> 인구 대비 병상 부족은 지역 위험도를 구성하는 하나의 요인이지만, 접근성 및 의료진 부족과 같은 다른 취약요인과 결합되었을 때 종합위험도 차이가 더욱 크게 나타났다.

---

# 10. 원천값 선형회귀 분석

## 10.1 사용 파일

회귀분석에는 `data/regression_result.csv`와 `data/regression_metrics.json`을 사용하였다.

종속변수는

```text
y = regionRisk
```

이며 독립변수는 다음 원천값이다.

```text
X =
포화율_원천
직선거리_km
인구대비병상비율
병상대비전문의부족비율
```

---

## 10.2 구성점수 대신 원천값을 사용한 이유

`regionRisk`는 이미 다음 구성점수의 가중합이다.

```text
병상포화도점수
접근성점수
인구대비병상점수
의료진부족점수
```

따라서 이 네 점수를 다시 독립변수로 사용해 `regionRisk`를 회귀분석하면, 사실상 위험도를 만든 값을 이용하여 다시 위험도를 설명하는 순환적인 분석이 된다.

이를 줄이기 위해 정규화 이전의 원천값을 사용하였다.

다만 원천값 역시 최종 구성점수를 만드는 기초 데이터이므로 본 회귀분석은 **외부 결과변수를 예측하는 모델이 아니라 위험도 산식의 민감도를 확인하는 분석**으로 해석해야 한다.

---

## 10.3 회귀계수

| 변수          |     회귀계수 |
| ----------- | -------: |
| 포화율_원천      |   0.3334 |
| 직선거리_km     |   0.6282 |
| 인구대비병상비율    | 0.000595 |
| 병상대비전문의부족비율 |   0.8945 |

모든 회귀계수의 부호가 양수이다.

즉, 현재 모델에서는 각각의 취약 원천값이 증가할수록 예측되는 `regionRisk`도 증가하는 방향을 나타낸다.

---

## 10.4 회귀계수 크기에 대한 주의

단순히

```text
0.8945 > 0.6282 > 0.3334 > 0.000595
```

라는 이유만으로

> 의료진 부족이 가장 중요하고 인구 대비 병상은 거의 중요하지 않다.

라고 결론 내리면 안 된다.

각 변수의 측정단위가 서로 다르기 때문이다.

예를 들어

```text
거리: km
포화율: %
병상비율: 특정 인구단위당 병상
전문의 부족비율: 병상/전문의
```

처럼 단위 자체가 다르기 때문에 회귀계수의 숫자를 직접 비교하기 어렵다.

변수의 상대적 영향력을 정확하게 비교하려면 원천변수를 표준화한 **표준화 회귀계수**를 추가로 산출하는 것이 적절하다.

---

# 11. 회귀모형 성능

## 11.1 R²

`data/regression_metrics.json`에 기록된 결정계수는

```text
R² = 0.833
```

이다.

이는 분석에 사용된 79개 지역에서 네 원천변수의 선형조합이 현재 `regionRisk` 변동의 약 **83.3%를 설명했다**는 의미이다.

그러나 다음과 같이 해석해서는 안 된다.

> "실제 응급환자의 위험을 83.3% 정확도로 예측하였다."

`regionRisk` 자체가 해당 원천변수들을 가공하여 만든 지표이기 때문이다.

따라서 정확한 해석은 다음과 같다.

> 원천변수의 선형조합이 현재 설계된 지역 위험도 지표의 변동을 비교적 높은 수준으로 재현하였다.

---

## 11.2 MAE

평균절대오차는

```text
MAE = 2.70
```

이다.

즉, 회귀모형으로 예측한 `regionRisk`와 실제 산식으로 계산된 `regionRisk` 사이에 평균적으로 약 **2.7점의 차이**가 있었다.

이 역시 실제 환자의 위험 예측오차가 아니라 **산출된 위험도 점수를 회귀모형으로 재현했을 때의 오차**이다.

---

# 12. K-Means 군집분석

## 12.1 분석 방법

최종 `regionRisk`가 산출된 지역을 대상으로 K-Means 군집분석을 실시하였다.

k=2부터 k=8까지 비교한 결과 현재 파이프라인에서는 **k=2가 최적 군집 수**로 선택되었다.

최종 군집분석에는 124개 지역이 포함되었다.

```text
Cluster 0 = 55개
Cluster 1 = 69개

총 124개
```

---

# 13. Cluster 0 분석

`data/cluster_profile.csv`에서 Cluster 0의 평균값은 다음과 같다.

| 변수         |        평균 |
| ---------- | --------: |
| 병상포화도점수    |     35.54 |
| 접근성점수      | **58.03** |
| 인구대비병상점수   |     19.38 |
| 의료진부족점수    | **94.79** |
| regionRisk | **47.94** |
| 지역 수       |        55 |

Cluster 1과 비교하면 의료진부족점수 차이는 약 **83.79점**, 접근성점수 차이는 약 **52.90점**이다.

반면 병상포화도는 Cluster 0이 오히려 Cluster 1보다 약 7.44점 낮다.

따라서 Cluster 0을 단순히 '병상 부족 지역'이라고 해석하는 것은 적절하지 않다.

이 군집의 가장 뚜렷한 특징은

* 높은 접근성 취약도
* 매우 높은 의료진 부족

이다.

따라서 본 분석에서는 Cluster 0을 다음과 같이 정의한다.

> **접근성·의료진 복합취약형**

---

# 14. Cluster 1 분석

Cluster 1의 평균값은 다음과 같다.

| 변수         |        평균 |
| ---------- | --------: |
| 병상포화도점수    |     42.98 |
| 접근성점수      |  **5.13** |
| 인구대비병상점수   |     38.59 |
| 의료진부족점수    | **11.00** |
| regionRisk | **25.95** |
| 지역 수       |        69 |

Cluster 1은 Cluster 0보다 병상포화도와 인구대비병상점수는 높지만, 접근성과 의료진부족점수가 현저하게 낮다.

따라서 다음과 같이 정의할 수 있다.

> **접근성 양호·병상부담형**

기존의 '도시형'이라는 표현도 가능하지만, Cluster 1에 속한 모든 지역이 실제 도시지역인 것은 아니므로 공식 보고서에서는 데이터 자체의 특성을 표현하는 **접근성 양호·병상부담형**이라는 표현을 사용한다.

---

# 15. 두 군집의 위험도 차이

Cluster 0의 평균 위험도는

```text
47.94
```

이고 Cluster 1은

```text
25.95
```

이다.

두 집단의 차이는 약

```text
47.94 - 25.95 = 21.99점
```

이다.

즉, 접근성과 의료진 부족이 동시에 높은 Cluster 0이 평균적으로 약 22점 높은 `regionRisk`를 나타냈다.

이 결과는 단순히 병상포화도가 높은 지역보다 **접근성 및 전문인력 취약성이 함께 존재하는 지역에서 종합 위험도가 높게 형성되고 있음**을 보여준다.

---

# 16. 대표적인 고위험 사례: 통영시

`data/cluster_result.csv`에서 통영시는 다음과 같다.

| 변수         |         값 |
| ---------- | --------: |
| 병상포화도점수    |    100.00 |
| 접근성점수      |     91.01 |
| 인구대비병상점수   |     54.05 |
| 의료진부족점수    |    100.00 |
| regionRisk | **88.11** |
| Cluster    |         0 |

통영시는 네 구성요소 가운데 세 가지가 매우 높은 수준이다.

특히

```text
병상포화도 = 100
접근성 = 91.01
의료진 부족 = 100
```

으로 나타났다.

따라서 통영시의 높은 위험도는 특정 하나의 요인이 아니라 **병상 부담 + 접근성 취약 + 전문의 부족이 동시에 발생한 복합위험**으로 해석할 수 있다.

---

# 17. 경상북도 MVP 분석

최종 서비스의 MVP 대상인 경상북도를 별도로 분석하였다.

`data/cluster_result.csv`에서 최종 위험도가 산출된 경상북도 지역은 다음과 같다.

| 지역  |  병상포화 |        접근성 | 인구대비병상 |      의료진부족 | regionRisk | 군집 |
| --- | ----: | ---------: | -----: | ---------: | ---------: | -: |
| 울릉군 | 40.00 | **100.00** |   0.00 | **100.00** |  **59.00** |  0 |
| 영주시 | 50.00 |      72.14 |  14.62 |     100.00 |  **57.07** |  0 |
| 울진군 | 37.50 | **100.00** |  16.02 |      43.53 |  **52.86** |  0 |
| 고령군 | 40.00 |      55.87 |  17.29 |     100.00 |  **49.22** |  0 |
| 영천시 | 20.00 |      65.48 |  36.83 |     100.00 |  **49.01** |  0 |
| 청송군 | 20.00 |      79.58 |  10.77 |     100.00 |  **48.03** |  0 |
| 상주시 | 27.78 |      45.18 |  12.61 |     100.00 |      40.80 |  0 |
| 예천군 |  0.00 |      59.64 |  33.17 |     100.00 |      39.53 |  0 |
| 청도군 |  0.00 |      62.42 |   7.25 |     100.00 |      35.17 |  0 |
| 포항시 | 38.67 |       3.98 |  29.21 |       1.71 |      20.83 |  1 |
| 김천시 | 47.24 |       1.90 |   7.48 |      10.88 |      20.23 |  1 |
| 문경시 | 35.29 |       0.00 |   6.72 |       9.79 |      15.17 |  1 |
| 안동시 | 31.11 |       1.89 |  10.24 |       1.09 |  **13.67** |  1 |

---

## 17.1 경상북도 위험도 상위 지역

최종 위험도가 산출된 지역만을 대상으로 하면 상위 지역은 다음과 같다.

1. 울릉군 — 59.00
2. 영주시 — 57.07
3. 울진군 — 52.86
4. 고령군 — 49.22
5. 영천시 — 49.01
6. 청송군 — 48.03

이 지역들을 보면 접근성점수가 대체로 높다.

또한 울진군을 제외한 상당수 지역에서 의료진부족점수가 100점으로 나타난다.

따라서 경상북도 위험지역에서는 **접근성 취약과 전문의 부족이 함께 나타나는 패턴**을 확인할 수 있다.

---

## 17.2 울릉군과 안동시 비교

경북에서 지역 간 차이를 보여주는 대표적인 사례로 울릉군과 안동시를 비교할 수 있다.

| 변수         |        울릉군 |       안동시 |
| ---------- | ---------: | --------: |
| 병상포화도      |      40.00 |     31.11 |
| 접근성        | **100.00** |  **1.89** |
| 인구대비병상     |       0.00 |     10.24 |
| 의료진 부족     | **100.00** |  **1.09** |
| regionRisk |  **59.00** | **13.67** |

병상포화도 차이는 약 8.89점이다.

하지만 최종 위험도 차이는 약

```text
59.00 - 13.67 = 45.33점
```

이다.

두 지역의 가장 큰 차이는 접근성과 의료진 부족에서 발생한다.

따라서 경북 MVP에서 단순히 위험도 숫자만 보여주는 것보다,

> "울릉군은 현재 병상포화 자체보다 응급의료센터 접근성과 전문의 부족 측면의 취약성이 크게 나타났습니다."

와 같이 **위험의 원인을 함께 제시하는 방식**이 데이터 활용 측면에서 더 의미가 있다.

---

# 18. 경상북도 순위 해석 시 주의사항

앞에서 제시한 순위는 **경상북도 전체 시군의 완전한 위험도 순위가 아니다.**

`data/region_risk_final.csv`를 확인하면 다음과 같이 최종 위험도가 산출되지 않은 지역이 존재한다.

예:

* 경산시
* 경주시
* 구미시
* 봉화군
* 영덕군
* 영양군
* 의성군
* 성주군 등

이들 지역은 일부 원천데이터가 부족하기 때문에 최종 `regionRisk`를 계산하지 않았다.

따라서 서비스와 보고서에서는 반드시 다음과 같이 표현해야 한다.

> **본 순위는 네 구성점수가 모두 확보되어 최종 위험도가 산출된 경상북도 지역만을 대상으로 한다. 데이터가 부족한 지역을 저위험 지역으로 간주하지 않는다.**

---

# 19. 서비스에서 활용할 수 있는 분석 결과

본 분석의 중요한 활용점은 단순한 지역 순위 제공이 아니다.

같은 `regionRisk`를 가진 지역이라도 위험의 원인은 다를 수 있다.

예를 들어 한 지역은

```text
병상포화 ↑
접근성 양호
전문의 충분
```

일 수 있고,

다른 지역은

```text
병상포화 낮음
접근성 매우 취약
전문의 부족
```

일 수 있다.

따라서 최종 사이트에서는 `regionRisk`와 함께 네 구성요소를 동시에 제공하는 것이 적절하다.

추천 표시 항목은 다음과 같다.

* 종합 `regionRisk`
* 위험등급
* 병상포화도점수
* 접근성점수
* 인구대비병상점수
* 의료진부족점수
* 해당 지역의 가장 큰 위험요인
* 군집 유형

예를 들어 울릉군은 다음과 같이 설명할 수 있다.

> **울릉군의 종합 응급의료 위험도는 59.0점입니다. 병상포화도는 40점이지만 접근성점수와 의료진부족점수가 각각 100점으로 높게 나타났습니다. 따라서 현재 데이터 기준 울릉군은 병상포화 자체보다 응급의료 접근성과 전문인력 부족이 주요 취약요인으로 분석됩니다.**

---

# 20. 분석의 한계

## 20.1 데이터가 부족한 지역

전체 219개 분석 지역 중 최종 `regionRisk`가 산출된 지역은 124개이다.

따라서 약 95개 지역은 적어도 하나의 구성요소가 부족하여 최종 위험도를 산출하지 않았다.

이 때문에 본 결과를 전국 모든 시군구에 대한 완전한 순위로 해석해서는 안 된다.

---

## 20.2 실시간 병상 데이터의 시점 의존성

병상포화도는 API를 실행한 특정 시점의 병상상태를 반영한다.

따라서 한 번의 실행결과가 해당 지역의 평상시 병상 상황을 대표한다고 보기 어렵다.

현재 `bed_status_history.csv`에 실행 시점별 데이터를 누적하고 있으나 장기적인 요일·시간대·계절 패턴을 분석하기에는 데이터가 충분하지 않다.

향후 파이프라인을 정기적으로 실행하여 데이터를 누적하면 시간대별 병상 위험도를 별도로 분석할 수 있다.

---

## 20.3 HIRA 병원 매칭 문제

NEMC의 `hpid`와 HIRA의 암호화 요양기호는 서로 다른 기관 식별자이다.

따라서 병원명과 주소를 이용하여 두 데이터의 기관을 연결하였다.

현재 자동매칭되지 않은 병원은 수동 검토 대상으로 관리하며, 확인되지 않은 기관을 억지로 매칭하지 않는다.

현행 파이프라인의 기준 모집단은 NEMC 534개 기관이며 HIRA는 LEFT JOIN 보강 원천이다. HIRA 매칭 실패 기관도 NEMC 모집단에서 제거하지 않고 의료인력 보강값만 결측 처리한다. 이 방법은 기준 모집단을 보존하고 잘못된 전문의 수 산출을 방지하지만, 네 구성점수가 모두 필요한 `regionRisk` 완료 지역 수는 줄어들 수 있다.

동일 이름으로 같은 HIRA 식별자에 연결됐던 고양 명지병원(`A2100042`)과 제천 명지병원(`A2300009`)은 공식 HIRA 기관 페이지를 근거로 수동검증했다. 현재 전문의 수는 각각 12명과 7명이며 별도 HIRA 식별자로 관리한다.

---

## 20.4 최신 행정경계와 데이터 연결

현행 인구 원천은 행정안전부 2026년 7월 주민등록 인구이며 NEMC의 219개 지역과 모두 연결된다. 과거 KOSIS 2024 경계 차이로 발생했던 인구 결측은 현재 산출물에서 해소되었다.

지도는 통계청 SGIS 행정경계를 기반으로 `admdongkor`가 가공한 `20260701` 시군구 경계 256개를 사용한다. 이 자료는 웹 시각화용 `light` 단순화 경계이며 법적·측량·주소 판정용 공식 원본이 아니다. 데이터 라이선스는 CC BY 4.0과 공공누리 제1유형(출처표시)이다.

경계 중 206개는 NEMC 지역과 직접 연결되며, NEMC가 상위 시로 집계한 13개 값은 최신 경계의 일반구 39개에 공통으로 표시된다. 따라서 이 39개 경계는 구별 독립 분석값이 아니다.

최신 경계 중 NEMC 응급의료기관이 없는 11개 지역은 데이터와 직접 대응하지 않는다. 이 지역은 저위험이 아니라 `NEMC 기관 없음/미산출`로 해석한다.

## 20.5 병원 좌표 보완

NEMC 원천에서 좌표가 없었던 새강병원(`A2502742`) 1건은 병원 공식 주소와 OpenStreetMap 병원 건물명·도로명을 교차검증한 좌표로 보완했다. 수동 좌표와 근거는 `data/hospital_coordinate_overrides.csv`에 보존하며, 현재 NEMC 534개 기관의 좌표 결측은 0개다.

---

# 21. 카카오 도로거리 변경 관련 검증 필요 사항

> [!WARNING]
> **최종 분석 결과 확정 전 반드시 확인해야 하는 항목이다.**

초기 접근성 분석에서는 지역과 응급의료기관 사이의 `직선거리_km`를 사용하였다.

이후 다음 스크립트를 통해 카카오모빌리티 자동차 길찾기 API 기반의 실제 도로거리 방식으로 접근성 파이프라인을 변경하였다.

```text
scripts/part3_collect_kakao_routes.py
```

현재 방식은 각 시군구 중심점에서 직선거리 기준 가까운 권역·지역응급의료센터 3개를 후보로 선정한 뒤, 카카오 자동차 길찾기 API를 호출하여 실제 도로거리가 가장 짧은 병원을 접근성 기준 병원으로 선택한다.

그러나 현재 제공된 `data/regression_result.csv`에는 접근성 원천변수가 다음과 같이 기록되어 있다.

```text
직선거리_km, 0.6281900669358561
```

따라서 다음 두 가능성을 확인해야 한다.

### 경우 1. 실제로 기존 직선거리 데이터를 사용한 경우

카카오 도로거리 도입 전에 생성된 회귀분석 결과가 그대로 남아 있는 것이다.

이 경우 카카오 도로거리 적용 후 PART 3 및 PART 4 분석을 다시 실행해야 한다.

### 경우 2. 데이터는 도로거리로 변경했지만 컬럼명만 그대로인 경우

실제 값은 카카오 도로거리이지만 코드 또는 CSV의 변수명만 `직선거리_km`로 남아 있을 수도 있다.

이 경우 분석값 자체는 사용할 수 있지만, 코드와 결과 파일의 컬럼명을 수정하여 혼동을 방지하는 것이 필요하다.

### 최종 확인 항목

카카오 도로거리 적용 후 다음 파일들이 모두 다시 생성되었는지 확인한다.

```text
카카오 도로거리 수집
        ↓
접근성점수 재계산
        ↓
regionRisk 재계산
        ↓
correlation_matrix.csv 재생성
        ↓
regression_result.csv 재생성
        ↓
cluster_result.csv 재생성
        ↓
cluster_profile.csv 재생성
```

따라서 본 보고서의 현재 수치는 **제공된 분석 결과를 기준으로 작성한 값이며, 카카오 도로거리 적용 이후 전체 파이프라인 재실행 여부를 확인한 뒤 최종 수치를 확정한다.**

---

# 22. 향후 추가 분석

현재 분석에서 다음 분석을 추가하면 지표의 신뢰성과 활용도를 더욱 높일 수 있다.

### 22.1 표준화 회귀분석

현재 회귀변수들은 서로 다른 단위를 사용한다.

따라서 원천변수를 표준화한 뒤 표준화 회귀계수를 산출하면 변수 간 상대적 영향력을 보다 적절하게 비교할 수 있다.

### 22.2 가중치 민감도 분석

현재 위험도 가중치는 다음과 같다.

```text
병상포화도       35%
접근성           30%
인구 대비 병상   20%
의료진 부족      15%
```

예를 들어 접근성 가중치를 30%에서 20% 또는 40%로 변경했을 때 지역 순위가 얼마나 변하는지 분석하면 현재 위험도 산식의 안정성을 확인할 수 있다.

### 22.3 외부 결과변수를 이용한 타당도 검증

현재 `regionRisk`의 실제 의미를 검증하기 위해서는 다음과 같은 외부 결과변수가 필요하다.

* 실제 응급환자 이송시간
* 병원 수용거절 건수
* 재이송률
* 응급실 장기체류시간
* 중증환자 사망률
* 치료가능 사망률
* 골든타임 내 의료기관 도착률

이러한 실제 결과와 `regionRisk`의 관계를 분석하면 현재 지표가 실제 응급의료 위험을 얼마나 잘 반영하는지 평가할 수 있다.

### 22.4 병상 이력 데이터 축적

현재 시점부터 병상 API를 정기적으로 실행하여 데이터를 축적하면

* 요일별 위험도
* 시간대별 위험도
* 주말·평일 차이
* 계절별 변화

등을 분석할 수 있다.

---

# 23. 최종 결론

본 프로젝트에서는 실시간 병상 현황, 응급의료 접근성, 인구 대비 병상 수준, 응급의학과 전문의 부족 수준을 통합하여 지역별 응급의료 위험도 `regionRisk`를 산출하였다.

`data/correlation_matrix.csv`를 분석한 결과 네 구성요소 중 `접근성점수`가 `regionRisk`와 가장 높은 상관관계인 **0.8009**를 보였으며, `의료진부족점수`도 **0.6731**의 비교적 높은 상관관계를 나타냈다.

특히 접근성점수와 의료진부족점수 사이에서도 **0.7837**의 높은 상관관계가 확인되었다.

이는 현재 분석 대상 지역에서 **응급의료 접근이 어려운 지역과 전문의 부족지역이 상당 부분 중첩되는 경향**이 있음을 보여준다.

K-Means 군집분석에서도 이러한 특징이 확인되었다.

Cluster 0은

* 접근성점수 평균 58.03
* 의료진부족점수 평균 94.79
* 평균 `regionRisk` 47.94

로 나타나 **접근성·의료진 복합취약형**으로 해석할 수 있었다.

반면 Cluster 1은

* 접근성점수 평균 5.13
* 의료진부족점수 평균 11.00
* 평균 `regionRisk` 25.95

로 나타나 **접근성 양호·병상부담형**으로 해석할 수 있었다.

경상북도 MVP에서도 동일한 패턴이 관찰되었다.

울릉군, 영주시, 울진군, 고령군, 영천시, 청송군 등 위험도가 높은 지역은 대체로 접근성 또는 의료진 부족 측면에서 높은 취약도를 보였다.

반면 안동시, 문경시, 김천시, 포항시는 접근성점수와 의료진부족점수가 상대적으로 낮았으며 최종 `regionRisk`도 낮게 나타났다.

따라서 본 프로젝트의 핵심적인 활용가치는 단순히 지역별 위험순위를 제공하는 데 있지 않다.

**각 지역의 종합적인 응급의료 위험도를 하나의 지표로 제시하면서 동시에 그 위험이 병상포화, 접근성, 인구 대비 병상, 의료진 부족 중 어떤 요인에서 발생하고 있는지를 구분하여 제공하는 것**에 의미가 있다.

이를 통해 사용자는 특정 지역이 단순히 '위험하다'는 사실뿐 아니라 **왜 위험한지**, 그리고 **어떤 의료자원의 취약성이 존재하는지**까지 함께 파악할 수 있다.

---

## 분석에 사용한 주요 결과 파일

* [`data/region_risk_final.csv`](data/region_risk_final.csv)
* [`data/correlation_matrix.csv`](data/correlation_matrix.csv)
* [`data/vif_result.csv`](data/vif_result.csv)
* [`data/regression_result.csv`](data/regression_result.csv)
* [`data/regression_metrics.json`](data/regression_metrics.json)
* [`data/cluster_k_evaluation.csv`](data/cluster_k_evaluation.csv)
* [`data/cluster_result.csv`](data/cluster_result.csv)
* [`data/cluster_profile.csv`](data/cluster_profile.csv)

---

**분석 기준:** 갱신 전 124개 완료지역 결과를 보존한 과거 분석 스냅샷

**현재 기준:** 최신 수치는 `data/region_risk_final.csv`, PART 4 결과 파일 및 `README.md`를 우선한다. 현행 회귀는 완료지역 196개 전체를 사용하며 R² `0.8613975747`, MAE `3.0653038392`다. HIRA 결측 보완이나 접근성 산식 변경 후에는 PART 4 결과와 본 보고서를 함께 다시 생성해야 한다.
