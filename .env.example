# Copy this file to `.env` for local development.
# Keep every secret blank in this tracked example and store production values in
# Railway Variables. Never prefix a secret with NEXT_PUBLIC_.
DATA_GO_KR_API_KEY=
HIRA_API_KEY=
KAKAO_REST_API_KEY=
PIPELINE_ADMIN_TOKEN=

# Local dynamic-runtime defaults. Railway automatically supplies
# RAILWAY_VOLUME_MOUNT_PATH when its volume is mounted at /app/runtime, so
# PIPELINE_RUNTIME_DIR does not need to be set there.
PIPELINE_RUNTIME_DIR=./runtime
ENABLE_PIPELINE_SCHEDULER=false
FAST_REFRESH_INTERVAL_MINUTES=60
FULL_REFRESH_INTERVAL_HOURS=24
PIPELINE_FAILURE_RETRY_MINUTES=15
BED_API_MAX_ATTEMPTS=3
RUN_FAST_REFRESH_ON_START=false
CLEAR_STALE_PIPELINE_LOCK_ON_START=false
BED_HISTORY_RETENTION_DAYS=30
NEXT_PUBLIC_DASHBOARD_POLL_SECONDS=60

# Railway-only: its volume is mounted as root while the image declares a
# non-root runtime user. Railway uses this setting to grant write access.
RAILWAY_RUN_UID=0
RAILWAY_DEPLOYMENT_DRAINING_SECONDS=30
